/*
 * This file automatically generated by shim.py.

 * EDIT AT YOUR PERIL.
 */
/*
 * MPI_* calls are weakly declared.  This allows us to 
 * link in our own function calls using the same name.
 * Instead of a namespace class, the weakly declared 
 * functions aren't used.  We access the normal MPI_*
 * functionality using the corresponding PMPI_* call.
 * Our intercept code (pre/post) is called before and
 * after the PMPI_* call.
 */
#include "shim_selection.h"
#include "shim_enumeration.h"
#include "shim.h"

//MPI_Accumulate
#ifdef USE_MPI_ACCUMULATE
int
MPI_Accumulate (void *origin_addr, int origin_count, MPI_Datatype origin_datatype, int target_rank, MPI_Aint target_disp, int target_count, MPI_Datatype target_datatype, MPI_Op op, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Accumulate_p.shim_id = GMPI_ACCUMULATE;
	shim.MPI_Accumulate_p.origin_addr = origin_addr;
	shim.MPI_Accumulate_p.origin_count = origin_count;
	shim.MPI_Accumulate_p.origin_datatype = origin_datatype;
	shim.MPI_Accumulate_p.target_rank = target_rank;
	shim.MPI_Accumulate_p.target_disp = target_disp;
	shim.MPI_Accumulate_p.target_count = target_count;
	shim.MPI_Accumulate_p.target_datatype = target_datatype;
	shim.MPI_Accumulate_p.op = op;
	shim.MPI_Accumulate_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Accumulate_p.rc = PMPI_Accumulate(origin_addr, origin_count, origin_datatype, target_rank, target_disp, target_count, target_datatype, op, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Accumulate_p.rc;
}
#endif //USE_MPI_ACCUMULATE

//MPI_Add_error_class
#ifdef USE_MPI_ADD_ERROR_CLASS
int
MPI_Add_error_class (int *errorclass){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Add_error_class_p.shim_id = GMPI_ADD_ERROR_CLASS;
	shim.MPI_Add_error_class_p.errorclass = errorclass;
	shim_pre( shim_id, &shim );
	shim.MPI_Add_error_class_p.rc = PMPI_Add_error_class(errorclass);
	shim_post( shim_id, &shim );
	return shim.MPI_Add_error_class_p.rc;
}
#endif //USE_MPI_ADD_ERROR_CLASS

//MPI_Add_error_code
#ifdef USE_MPI_ADD_ERROR_CODE
int
MPI_Add_error_code (int errorclass, int *errorcode){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Add_error_code_p.shim_id = GMPI_ADD_ERROR_CODE;
	shim.MPI_Add_error_code_p.errorclass = errorclass;
	shim.MPI_Add_error_code_p.errorcode = errorcode;
	shim_pre( shim_id, &shim );
	shim.MPI_Add_error_code_p.rc = PMPI_Add_error_code(errorclass, errorcode);
	shim_post( shim_id, &shim );
	return shim.MPI_Add_error_code_p.rc;
}
#endif //USE_MPI_ADD_ERROR_CODE

//MPI_Add_error_string
#ifdef USE_MPI_ADD_ERROR_STRING
int
MPI_Add_error_string (int errorcode, char *string){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Add_error_string_p.shim_id = GMPI_ADD_ERROR_STRING;
	shim.MPI_Add_error_string_p.errorcode = errorcode;
	shim.MPI_Add_error_string_p.string = string;
	shim_pre( shim_id, &shim );
	shim.MPI_Add_error_string_p.rc = PMPI_Add_error_string(errorcode, string);
	shim_post( shim_id, &shim );
	return shim.MPI_Add_error_string_p.rc;
}
#endif //USE_MPI_ADD_ERROR_STRING

//MPI_Address
#ifdef USE_MPI_ADDRESS
int
MPI_Address (void *location, MPI_Aint *address){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Address_p.shim_id = GMPI_ADDRESS;
	shim.MPI_Address_p.location = location;
	shim.MPI_Address_p.address = address;
	shim_pre( shim_id, &shim );
	shim.MPI_Address_p.rc = PMPI_Address(location, address);
	shim_post( shim_id, &shim );
	return shim.MPI_Address_p.rc;
}
#endif //USE_MPI_ADDRESS

//MPI_Allgather
#ifdef USE_MPI_ALLGATHER
int
MPI_Allgather (void *sendbuf, int sendcount, MPI_Datatype sendtype, void *recvbuf, int recvcount, MPI_Datatype recvtype, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Allgather_p.shim_id = GMPI_ALLGATHER;
	shim.MPI_Allgather_p.sendbuf = sendbuf;
	shim.MPI_Allgather_p.sendcount = sendcount;
	shim.MPI_Allgather_p.sendtype = sendtype;
	shim.MPI_Allgather_p.recvbuf = recvbuf;
	shim.MPI_Allgather_p.recvcount = recvcount;
	shim.MPI_Allgather_p.recvtype = recvtype;
	shim.MPI_Allgather_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Allgather_p.rc = PMPI_Allgather(sendbuf, sendcount, sendtype, recvbuf, recvcount, recvtype, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Allgather_p.rc;
}
#endif //USE_MPI_ALLGATHER

//MPI_Allgatherv
#ifdef USE_MPI_ALLGATHERV
int
MPI_Allgatherv (void *sendbuf, int sendcount, MPI_Datatype sendtype, void *recvbuf, int *recvcounts, int *displs, MPI_Datatype recvtype, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Allgatherv_p.shim_id = GMPI_ALLGATHERV;
	shim.MPI_Allgatherv_p.sendbuf = sendbuf;
	shim.MPI_Allgatherv_p.sendcount = sendcount;
	shim.MPI_Allgatherv_p.sendtype = sendtype;
	shim.MPI_Allgatherv_p.recvbuf = recvbuf;
	shim.MPI_Allgatherv_p.recvcounts = recvcounts;
	shim.MPI_Allgatherv_p.displs = displs;
	shim.MPI_Allgatherv_p.recvtype = recvtype;
	shim.MPI_Allgatherv_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Allgatherv_p.rc = PMPI_Allgatherv(sendbuf, sendcount, sendtype, recvbuf, recvcounts, displs, recvtype, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Allgatherv_p.rc;
}
#endif //USE_MPI_ALLGATHERV

//MPI_Alloc_mem
#ifdef USE_MPI_ALLOC_MEM
int
MPI_Alloc_mem (MPI_Aint size, MPI_Info info, void *baseptr){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Alloc_mem_p.shim_id = GMPI_ALLOC_MEM;
	shim.MPI_Alloc_mem_p.size = size;
	shim.MPI_Alloc_mem_p.info = info;
	shim.MPI_Alloc_mem_p.baseptr = baseptr;
	shim_pre( shim_id, &shim );
	shim.MPI_Alloc_mem_p.rc = PMPI_Alloc_mem(size, info, baseptr);
	shim_post( shim_id, &shim );
	return shim.MPI_Alloc_mem_p.rc;
}
#endif //USE_MPI_ALLOC_MEM

//MPI_Allreduce
#ifdef USE_MPI_ALLREDUCE
int
MPI_Allreduce (void *sendbuf, void *recvbuf, int count, MPI_Datatype datatype, MPI_Op op, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Allreduce_p.shim_id = GMPI_ALLREDUCE;
	shim.MPI_Allreduce_p.sendbuf = sendbuf;
	shim.MPI_Allreduce_p.recvbuf = recvbuf;
	shim.MPI_Allreduce_p.count = count;
	shim.MPI_Allreduce_p.datatype = datatype;
	shim.MPI_Allreduce_p.op = op;
	shim.MPI_Allreduce_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Allreduce_p.rc = PMPI_Allreduce(sendbuf, recvbuf, count, datatype, op, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Allreduce_p.rc;
}
#endif //USE_MPI_ALLREDUCE

//MPI_Alltoall
#ifdef USE_MPI_ALLTOALL
int
MPI_Alltoall (void *sendbuf, int sendcount, MPI_Datatype sendtype, void *recvbuf, int recvcount, MPI_Datatype recvtype, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Alltoall_p.shim_id = GMPI_ALLTOALL;
	shim.MPI_Alltoall_p.sendbuf = sendbuf;
	shim.MPI_Alltoall_p.sendcount = sendcount;
	shim.MPI_Alltoall_p.sendtype = sendtype;
	shim.MPI_Alltoall_p.recvbuf = recvbuf;
	shim.MPI_Alltoall_p.recvcount = recvcount;
	shim.MPI_Alltoall_p.recvtype = recvtype;
	shim.MPI_Alltoall_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Alltoall_p.rc = PMPI_Alltoall(sendbuf, sendcount, sendtype, recvbuf, recvcount, recvtype, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Alltoall_p.rc;
}
#endif //USE_MPI_ALLTOALL

//MPI_Alltoallv
#ifdef USE_MPI_ALLTOALLV
int
MPI_Alltoallv (void *sendbuf, int *sendcounts, int *sdispls, MPI_Datatype sendtype, void *recvbuf, int *recvcounts, int *rdispls, MPI_Datatype recvtype, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Alltoallv_p.shim_id = GMPI_ALLTOALLV;
	shim.MPI_Alltoallv_p.sendbuf = sendbuf;
	shim.MPI_Alltoallv_p.sendcounts = sendcounts;
	shim.MPI_Alltoallv_p.sdispls = sdispls;
	shim.MPI_Alltoallv_p.sendtype = sendtype;
	shim.MPI_Alltoallv_p.recvbuf = recvbuf;
	shim.MPI_Alltoallv_p.recvcounts = recvcounts;
	shim.MPI_Alltoallv_p.rdispls = rdispls;
	shim.MPI_Alltoallv_p.recvtype = recvtype;
	shim.MPI_Alltoallv_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Alltoallv_p.rc = PMPI_Alltoallv(sendbuf, sendcounts, sdispls, sendtype, recvbuf, recvcounts, rdispls, recvtype, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Alltoallv_p.rc;
}
#endif //USE_MPI_ALLTOALLV

//MPI_Alltoallw
#ifdef USE_MPI_ALLTOALLW
int
MPI_Alltoallw (void *sendbuf, int *sendcounts, int *sdispls, MPI_Datatype *sendtypes, void *recvbuf, int *recvcounts, int *rdispls, MPI_Datatype *recvtypes, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Alltoallw_p.shim_id = GMPI_ALLTOALLW;
	shim.MPI_Alltoallw_p.sendbuf = sendbuf;
	shim.MPI_Alltoallw_p.sendcounts = sendcounts;
	shim.MPI_Alltoallw_p.sdispls = sdispls;
	shim.MPI_Alltoallw_p.sendtypes = sendtypes;
	shim.MPI_Alltoallw_p.recvbuf = recvbuf;
	shim.MPI_Alltoallw_p.recvcounts = recvcounts;
	shim.MPI_Alltoallw_p.rdispls = rdispls;
	shim.MPI_Alltoallw_p.recvtypes = recvtypes;
	shim.MPI_Alltoallw_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Alltoallw_p.rc = PMPI_Alltoallw(sendbuf, sendcounts, sdispls, sendtypes, recvbuf, recvcounts, rdispls, recvtypes, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Alltoallw_p.rc;
}
#endif //USE_MPI_ALLTOALLW

//MPI_Attr_delete
#ifdef USE_MPI_ATTR_DELETE
int
MPI_Attr_delete (MPI_Comm comm, int keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Attr_delete_p.shim_id = GMPI_ATTR_DELETE;
	shim.MPI_Attr_delete_p.comm = comm;
	shim.MPI_Attr_delete_p.keyval = keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Attr_delete_p.rc = PMPI_Attr_delete(comm, keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Attr_delete_p.rc;
}
#endif //USE_MPI_ATTR_DELETE

//MPI_Attr_get
#ifdef USE_MPI_ATTR_GET
int
MPI_Attr_get (MPI_Comm comm, int keyval, void *attribute_val, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Attr_get_p.shim_id = GMPI_ATTR_GET;
	shim.MPI_Attr_get_p.comm = comm;
	shim.MPI_Attr_get_p.keyval = keyval;
	shim.MPI_Attr_get_p.attribute_val = attribute_val;
	shim.MPI_Attr_get_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Attr_get_p.rc = PMPI_Attr_get(comm, keyval, attribute_val, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Attr_get_p.rc;
}
#endif //USE_MPI_ATTR_GET

//MPI_Attr_put
#ifdef USE_MPI_ATTR_PUT
int
MPI_Attr_put (MPI_Comm comm, int keyval, void *attribute_val){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Attr_put_p.shim_id = GMPI_ATTR_PUT;
	shim.MPI_Attr_put_p.comm = comm;
	shim.MPI_Attr_put_p.keyval = keyval;
	shim.MPI_Attr_put_p.attribute_val = attribute_val;
	shim_pre( shim_id, &shim );
	shim.MPI_Attr_put_p.rc = PMPI_Attr_put(comm, keyval, attribute_val);
	shim_post( shim_id, &shim );
	return shim.MPI_Attr_put_p.rc;
}
#endif //USE_MPI_ATTR_PUT

//MPI_Barrier
#ifdef USE_MPI_BARRIER
int
MPI_Barrier (MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Barrier_p.shim_id = GMPI_BARRIER;
	shim.MPI_Barrier_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Barrier_p.rc = PMPI_Barrier(comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Barrier_p.rc;
}
#endif //USE_MPI_BARRIER

//MPI_Bcast
#ifdef USE_MPI_BCAST
int
MPI_Bcast (void *buffer, int count, MPI_Datatype datatype, int root, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Bcast_p.shim_id = GMPI_BCAST;
	shim.MPI_Bcast_p.buffer = buffer;
	shim.MPI_Bcast_p.count = count;
	shim.MPI_Bcast_p.datatype = datatype;
	shim.MPI_Bcast_p.root = root;
	shim.MPI_Bcast_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Bcast_p.rc = PMPI_Bcast(buffer, count, datatype, root, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Bcast_p.rc;
}
#endif //USE_MPI_BCAST

//MPI_Bsend
#ifdef USE_MPI_BSEND
int
MPI_Bsend (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Bsend_p.shim_id = GMPI_BSEND;
	shim.MPI_Bsend_p.buf = buf;
	shim.MPI_Bsend_p.count = count;
	shim.MPI_Bsend_p.datatype = datatype;
	shim.MPI_Bsend_p.dest = dest;
	shim.MPI_Bsend_p.tag = tag;
	shim.MPI_Bsend_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Bsend_p.rc = PMPI_Bsend(buf, count, datatype, dest, tag, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Bsend_p.rc;
}
#endif //USE_MPI_BSEND

//MPI_Bsend_init
#ifdef USE_MPI_BSEND_INIT
int
MPI_Bsend_init (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Bsend_init_p.shim_id = GMPI_BSEND_INIT;
	shim.MPI_Bsend_init_p.buf = buf;
	shim.MPI_Bsend_init_p.count = count;
	shim.MPI_Bsend_init_p.datatype = datatype;
	shim.MPI_Bsend_init_p.dest = dest;
	shim.MPI_Bsend_init_p.tag = tag;
	shim.MPI_Bsend_init_p.comm = comm;
	shim.MPI_Bsend_init_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Bsend_init_p.rc = PMPI_Bsend_init(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Bsend_init_p.rc;
}
#endif //USE_MPI_BSEND_INIT

//MPI_Buffer_attach
#ifdef USE_MPI_BUFFER_ATTACH
int
MPI_Buffer_attach (void *buffer, int size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Buffer_attach_p.shim_id = GMPI_BUFFER_ATTACH;
	shim.MPI_Buffer_attach_p.buffer = buffer;
	shim.MPI_Buffer_attach_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Buffer_attach_p.rc = PMPI_Buffer_attach(buffer, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Buffer_attach_p.rc;
}
#endif //USE_MPI_BUFFER_ATTACH

//MPI_Buffer_detach
#ifdef USE_MPI_BUFFER_DETACH
int
MPI_Buffer_detach (void *buffer, int *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Buffer_detach_p.shim_id = GMPI_BUFFER_DETACH;
	shim.MPI_Buffer_detach_p.buffer = buffer;
	shim.MPI_Buffer_detach_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Buffer_detach_p.rc = PMPI_Buffer_detach(buffer, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Buffer_detach_p.rc;
}
#endif //USE_MPI_BUFFER_DETACH

//MPI_Cancel
#ifdef USE_MPI_CANCEL
int
MPI_Cancel (MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cancel_p.shim_id = GMPI_CANCEL;
	shim.MPI_Cancel_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Cancel_p.rc = PMPI_Cancel(request);
	shim_post( shim_id, &shim );
	return shim.MPI_Cancel_p.rc;
}
#endif //USE_MPI_CANCEL

//MPI_Cart_coords
#ifdef USE_MPI_CART_COORDS
int
MPI_Cart_coords (MPI_Comm comm, int rank, int maxdims, int *coords){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_coords_p.shim_id = GMPI_CART_COORDS;
	shim.MPI_Cart_coords_p.comm = comm;
	shim.MPI_Cart_coords_p.rank = rank;
	shim.MPI_Cart_coords_p.maxdims = maxdims;
	shim.MPI_Cart_coords_p.coords = coords;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_coords_p.rc = PMPI_Cart_coords(comm, rank, maxdims, coords);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_coords_p.rc;
}
#endif //USE_MPI_CART_COORDS

//MPI_Cart_create
#ifdef USE_MPI_CART_CREATE
int
MPI_Cart_create (MPI_Comm old_comm, int ndims, int *dims, int *periods, int reorder, MPI_Comm *comm_cart){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_create_p.shim_id = GMPI_CART_CREATE;
	shim.MPI_Cart_create_p.old_comm = old_comm;
	shim.MPI_Cart_create_p.ndims = ndims;
	shim.MPI_Cart_create_p.dims = dims;
	shim.MPI_Cart_create_p.periods = periods;
	shim.MPI_Cart_create_p.reorder = reorder;
	shim.MPI_Cart_create_p.comm_cart = comm_cart;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_create_p.rc = PMPI_Cart_create(old_comm, ndims, dims, periods, reorder, comm_cart);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_create_p.rc;
}
#endif //USE_MPI_CART_CREATE

//MPI_Cart_get
#ifdef USE_MPI_CART_GET
int
MPI_Cart_get (MPI_Comm comm, int maxdims, int *dims, int *periods, int *coords){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_get_p.shim_id = GMPI_CART_GET;
	shim.MPI_Cart_get_p.comm = comm;
	shim.MPI_Cart_get_p.maxdims = maxdims;
	shim.MPI_Cart_get_p.dims = dims;
	shim.MPI_Cart_get_p.periods = periods;
	shim.MPI_Cart_get_p.coords = coords;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_get_p.rc = PMPI_Cart_get(comm, maxdims, dims, periods, coords);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_get_p.rc;
}
#endif //USE_MPI_CART_GET

//MPI_Cart_map
#ifdef USE_MPI_CART_MAP
int
MPI_Cart_map (MPI_Comm comm, int ndims, int *dims, int *periods, int *newrank){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_map_p.shim_id = GMPI_CART_MAP;
	shim.MPI_Cart_map_p.comm = comm;
	shim.MPI_Cart_map_p.ndims = ndims;
	shim.MPI_Cart_map_p.dims = dims;
	shim.MPI_Cart_map_p.periods = periods;
	shim.MPI_Cart_map_p.newrank = newrank;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_map_p.rc = PMPI_Cart_map(comm, ndims, dims, periods, newrank);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_map_p.rc;
}
#endif //USE_MPI_CART_MAP

//MPI_Cart_rank
#ifdef USE_MPI_CART_RANK
int
MPI_Cart_rank (MPI_Comm comm, int *coords, int *rank){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_rank_p.shim_id = GMPI_CART_RANK;
	shim.MPI_Cart_rank_p.comm = comm;
	shim.MPI_Cart_rank_p.coords = coords;
	shim.MPI_Cart_rank_p.rank = rank;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_rank_p.rc = PMPI_Cart_rank(comm, coords, rank);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_rank_p.rc;
}
#endif //USE_MPI_CART_RANK

//MPI_Cart_shift
#ifdef USE_MPI_CART_SHIFT
int
MPI_Cart_shift (MPI_Comm comm, int direction, int disp, int *rank_source, int *rank_dest){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_shift_p.shim_id = GMPI_CART_SHIFT;
	shim.MPI_Cart_shift_p.comm = comm;
	shim.MPI_Cart_shift_p.direction = direction;
	shim.MPI_Cart_shift_p.disp = disp;
	shim.MPI_Cart_shift_p.rank_source = rank_source;
	shim.MPI_Cart_shift_p.rank_dest = rank_dest;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_shift_p.rc = PMPI_Cart_shift(comm, direction, disp, rank_source, rank_dest);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_shift_p.rc;
}
#endif //USE_MPI_CART_SHIFT

//MPI_Cart_sub
#ifdef USE_MPI_CART_SUB
int
MPI_Cart_sub (MPI_Comm comm, int *remain_dims, MPI_Comm *new_comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cart_sub_p.shim_id = GMPI_CART_SUB;
	shim.MPI_Cart_sub_p.comm = comm;
	shim.MPI_Cart_sub_p.remain_dims = remain_dims;
	shim.MPI_Cart_sub_p.new_comm = new_comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Cart_sub_p.rc = PMPI_Cart_sub(comm, remain_dims, new_comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Cart_sub_p.rc;
}
#endif //USE_MPI_CART_SUB

//MPI_Cartdim_get
#ifdef USE_MPI_CARTDIM_GET
int
MPI_Cartdim_get (MPI_Comm comm, int *ndims){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Cartdim_get_p.shim_id = GMPI_CARTDIM_GET;
	shim.MPI_Cartdim_get_p.comm = comm;
	shim.MPI_Cartdim_get_p.ndims = ndims;
	shim_pre( shim_id, &shim );
	shim.MPI_Cartdim_get_p.rc = PMPI_Cartdim_get(comm, ndims);
	shim_post( shim_id, &shim );
	return shim.MPI_Cartdim_get_p.rc;
}
#endif //USE_MPI_CARTDIM_GET

//MPI_Close_port
#ifdef USE_MPI_CLOSE_PORT
int
MPI_Close_port (char *port_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Close_port_p.shim_id = GMPI_CLOSE_PORT;
	shim.MPI_Close_port_p.port_name = port_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Close_port_p.rc = PMPI_Close_port(port_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Close_port_p.rc;
}
#endif //USE_MPI_CLOSE_PORT

//MPI_Comm_accept
#ifdef USE_MPI_COMM_ACCEPT
int
MPI_Comm_accept (char *port_name, MPI_Info info, int root, MPI_Comm comm, MPI_Comm *newcomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_accept_p.shim_id = GMPI_COMM_ACCEPT;
	shim.MPI_Comm_accept_p.port_name = port_name;
	shim.MPI_Comm_accept_p.info = info;
	shim.MPI_Comm_accept_p.root = root;
	shim.MPI_Comm_accept_p.comm = comm;
	shim.MPI_Comm_accept_p.newcomm = newcomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_accept_p.rc = PMPI_Comm_accept(port_name, info, root, comm, newcomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_accept_p.rc;
}
#endif //USE_MPI_COMM_ACCEPT

//MPI_Comm_c2f
#ifdef USE_MPI_COMM_C2F
int
MPI_Comm_c2f (MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_c2f_p.shim_id = GMPI_COMM_C2F;
	shim.MPI_Comm_c2f_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_c2f_p.rc = PMPI_Comm_c2f(comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_c2f_p.rc;
}
#endif //USE_MPI_COMM_C2F

//MPI_Comm_call_errhandler
#ifdef USE_MPI_COMM_CALL_ERRHANDLER
int
MPI_Comm_call_errhandler (MPI_Comm comm, int errorcode){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_call_errhandler_p.shim_id = GMPI_COMM_CALL_ERRHANDLER;
	shim.MPI_Comm_call_errhandler_p.comm = comm;
	shim.MPI_Comm_call_errhandler_p.errorcode = errorcode;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_call_errhandler_p.rc = PMPI_Comm_call_errhandler(comm, errorcode);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_call_errhandler_p.rc;
}
#endif //USE_MPI_COMM_CALL_ERRHANDLER

//MPI_Comm_compare
#ifdef USE_MPI_COMM_COMPARE
int
MPI_Comm_compare (MPI_Comm comm1, MPI_Comm comm2, int *result){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_compare_p.shim_id = GMPI_COMM_COMPARE;
	shim.MPI_Comm_compare_p.comm1 = comm1;
	shim.MPI_Comm_compare_p.comm2 = comm2;
	shim.MPI_Comm_compare_p.result = result;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_compare_p.rc = PMPI_Comm_compare(comm1, comm2, result);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_compare_p.rc;
}
#endif //USE_MPI_COMM_COMPARE

//MPI_Comm_connect
#ifdef USE_MPI_COMM_CONNECT
int
MPI_Comm_connect (char *port_name, MPI_Info info, int root, MPI_Comm comm, MPI_Comm *newcomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_connect_p.shim_id = GMPI_COMM_CONNECT;
	shim.MPI_Comm_connect_p.port_name = port_name;
	shim.MPI_Comm_connect_p.info = info;
	shim.MPI_Comm_connect_p.root = root;
	shim.MPI_Comm_connect_p.comm = comm;
	shim.MPI_Comm_connect_p.newcomm = newcomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_connect_p.rc = PMPI_Comm_connect(port_name, info, root, comm, newcomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_connect_p.rc;
}
#endif //USE_MPI_COMM_CONNECT

//MPI_Comm_create_errhandler
#ifdef USE_MPI_COMM_CREATE_ERRHANDLER
int
MPI_Comm_create_errhandler (MPI_Comm_errhandler_fn *function, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_create_errhandler_p.shim_id = GMPI_COMM_CREATE_ERRHANDLER;
	shim.MPI_Comm_create_errhandler_p.function = function;
	shim.MPI_Comm_create_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_create_errhandler_p.rc = PMPI_Comm_create_errhandler(function, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_create_errhandler_p.rc;
}
#endif //USE_MPI_COMM_CREATE_ERRHANDLER

//MPI_Comm_create_keyval
#ifdef USE_MPI_COMM_CREATE_KEYVAL
int
MPI_Comm_create_keyval (MPI_Comm_copy_attr_function *comm_copy_attr_fn, MPI_Comm_delete_attr_function *comm_delete_attr_fn, int *comm_keyval, void *extra_state){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_create_keyval_p.shim_id = GMPI_COMM_CREATE_KEYVAL;
	shim.MPI_Comm_create_keyval_p.comm_copy_attr_fn = comm_copy_attr_fn;
	shim.MPI_Comm_create_keyval_p.comm_delete_attr_fn = comm_delete_attr_fn;
	shim.MPI_Comm_create_keyval_p.comm_keyval = comm_keyval;
	shim.MPI_Comm_create_keyval_p.extra_state = extra_state;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_create_keyval_p.rc = PMPI_Comm_create_keyval(comm_copy_attr_fn, comm_delete_attr_fn, comm_keyval, extra_state);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_create_keyval_p.rc;
}
#endif //USE_MPI_COMM_CREATE_KEYVAL

//MPI_Comm_create
#ifdef USE_MPI_COMM_CREATE
int
MPI_Comm_create (MPI_Comm comm, MPI_Group group, MPI_Comm *newcomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_create_p.shim_id = GMPI_COMM_CREATE;
	shim.MPI_Comm_create_p.comm = comm;
	shim.MPI_Comm_create_p.group = group;
	shim.MPI_Comm_create_p.newcomm = newcomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_create_p.rc = PMPI_Comm_create(comm, group, newcomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_create_p.rc;
}
#endif //USE_MPI_COMM_CREATE

//MPI_Comm_delete_attr
#ifdef USE_MPI_COMM_DELETE_ATTR
int
MPI_Comm_delete_attr (MPI_Comm comm, int comm_keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_delete_attr_p.shim_id = GMPI_COMM_DELETE_ATTR;
	shim.MPI_Comm_delete_attr_p.comm = comm;
	shim.MPI_Comm_delete_attr_p.comm_keyval = comm_keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_delete_attr_p.rc = PMPI_Comm_delete_attr(comm, comm_keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_delete_attr_p.rc;
}
#endif //USE_MPI_COMM_DELETE_ATTR

//MPI_Comm_disconnect
#ifdef USE_MPI_COMM_DISCONNECT
int
MPI_Comm_disconnect (MPI_Comm *comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_disconnect_p.shim_id = GMPI_COMM_DISCONNECT;
	shim.MPI_Comm_disconnect_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_disconnect_p.rc = PMPI_Comm_disconnect(comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_disconnect_p.rc;
}
#endif //USE_MPI_COMM_DISCONNECT

//MPI_Comm_dup
#ifdef USE_MPI_COMM_DUP
int
MPI_Comm_dup (MPI_Comm comm, MPI_Comm *newcomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_dup_p.shim_id = GMPI_COMM_DUP;
	shim.MPI_Comm_dup_p.comm = comm;
	shim.MPI_Comm_dup_p.newcomm = newcomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_dup_p.rc = PMPI_Comm_dup(comm, newcomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_dup_p.rc;
}
#endif //USE_MPI_COMM_DUP

//MPI_Comm_f2c
#ifdef USE_MPI_COMM_F2C
MPI_Comm
MPI_Comm_f2c (int comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_f2c_p.shim_id = GMPI_COMM_F2C;
	shim.MPI_Comm_f2c_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_f2c_p.rc = PMPI_Comm_f2c(comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_f2c_p.rc;
}
#endif //USE_MPI_COMM_F2C

//MPI_Comm_free_keyval
#ifdef USE_MPI_COMM_FREE_KEYVAL
int
MPI_Comm_free_keyval (int *comm_keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_free_keyval_p.shim_id = GMPI_COMM_FREE_KEYVAL;
	shim.MPI_Comm_free_keyval_p.comm_keyval = comm_keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_free_keyval_p.rc = PMPI_Comm_free_keyval(comm_keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_free_keyval_p.rc;
}
#endif //USE_MPI_COMM_FREE_KEYVAL

//MPI_Comm_free
#ifdef USE_MPI_COMM_FREE
int
MPI_Comm_free (MPI_Comm *comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_free_p.shim_id = GMPI_COMM_FREE;
	shim.MPI_Comm_free_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_free_p.rc = PMPI_Comm_free(comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_free_p.rc;
}
#endif //USE_MPI_COMM_FREE

//MPI_Comm_get_attr
#ifdef USE_MPI_COMM_GET_ATTR
int
MPI_Comm_get_attr (MPI_Comm comm, int comm_keyval, void *attribute_val, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_get_attr_p.shim_id = GMPI_COMM_GET_ATTR;
	shim.MPI_Comm_get_attr_p.comm = comm;
	shim.MPI_Comm_get_attr_p.comm_keyval = comm_keyval;
	shim.MPI_Comm_get_attr_p.attribute_val = attribute_val;
	shim.MPI_Comm_get_attr_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_get_attr_p.rc = PMPI_Comm_get_attr(comm, comm_keyval, attribute_val, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_get_attr_p.rc;
}
#endif //USE_MPI_COMM_GET_ATTR

//MPI_Comm_get_errhandler
#ifdef USE_MPI_COMM_GET_ERRHANDLER
int
MPI_Comm_get_errhandler (MPI_Comm comm, MPI_Errhandler *erhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_get_errhandler_p.shim_id = GMPI_COMM_GET_ERRHANDLER;
	shim.MPI_Comm_get_errhandler_p.comm = comm;
	shim.MPI_Comm_get_errhandler_p.erhandler = erhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_get_errhandler_p.rc = PMPI_Comm_get_errhandler(comm, erhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_get_errhandler_p.rc;
}
#endif //USE_MPI_COMM_GET_ERRHANDLER

//MPI_Comm_get_name
#ifdef USE_MPI_COMM_GET_NAME
int
MPI_Comm_get_name (MPI_Comm comm, char *comm_name, int *resultlen){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_get_name_p.shim_id = GMPI_COMM_GET_NAME;
	shim.MPI_Comm_get_name_p.comm = comm;
	shim.MPI_Comm_get_name_p.comm_name = comm_name;
	shim.MPI_Comm_get_name_p.resultlen = resultlen;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_get_name_p.rc = PMPI_Comm_get_name(comm, comm_name, resultlen);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_get_name_p.rc;
}
#endif //USE_MPI_COMM_GET_NAME

//MPI_Comm_get_parent
#ifdef USE_MPI_COMM_GET_PARENT
int
MPI_Comm_get_parent (MPI_Comm *parent){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_get_parent_p.shim_id = GMPI_COMM_GET_PARENT;
	shim.MPI_Comm_get_parent_p.parent = parent;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_get_parent_p.rc = PMPI_Comm_get_parent(parent);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_get_parent_p.rc;
}
#endif //USE_MPI_COMM_GET_PARENT

//MPI_Comm_group
#ifdef USE_MPI_COMM_GROUP
int
MPI_Comm_group (MPI_Comm comm, MPI_Group *group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_group_p.shim_id = GMPI_COMM_GROUP;
	shim.MPI_Comm_group_p.comm = comm;
	shim.MPI_Comm_group_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_group_p.rc = PMPI_Comm_group(comm, group);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_group_p.rc;
}
#endif //USE_MPI_COMM_GROUP

//MPI_Comm_join
#ifdef USE_MPI_COMM_JOIN
int
MPI_Comm_join (int fd, MPI_Comm *intercomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_join_p.shim_id = GMPI_COMM_JOIN;
	shim.MPI_Comm_join_p.fd = fd;
	shim.MPI_Comm_join_p.intercomm = intercomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_join_p.rc = PMPI_Comm_join(fd, intercomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_join_p.rc;
}
#endif //USE_MPI_COMM_JOIN

//MPI_Comm_rank
#ifdef USE_MPI_COMM_RANK
int
MPI_Comm_rank (MPI_Comm comm, int *rank){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_rank_p.shim_id = GMPI_COMM_RANK;
	shim.MPI_Comm_rank_p.comm = comm;
	shim.MPI_Comm_rank_p.rank = rank;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_rank_p.rc = PMPI_Comm_rank(comm, rank);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_rank_p.rc;
}
#endif //USE_MPI_COMM_RANK

//MPI_Comm_remote_group
#ifdef USE_MPI_COMM_REMOTE_GROUP
int
MPI_Comm_remote_group (MPI_Comm comm, MPI_Group *group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_remote_group_p.shim_id = GMPI_COMM_REMOTE_GROUP;
	shim.MPI_Comm_remote_group_p.comm = comm;
	shim.MPI_Comm_remote_group_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_remote_group_p.rc = PMPI_Comm_remote_group(comm, group);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_remote_group_p.rc;
}
#endif //USE_MPI_COMM_REMOTE_GROUP

//MPI_Comm_remote_size
#ifdef USE_MPI_COMM_REMOTE_SIZE
int
MPI_Comm_remote_size (MPI_Comm comm, int *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_remote_size_p.shim_id = GMPI_COMM_REMOTE_SIZE;
	shim.MPI_Comm_remote_size_p.comm = comm;
	shim.MPI_Comm_remote_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_remote_size_p.rc = PMPI_Comm_remote_size(comm, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_remote_size_p.rc;
}
#endif //USE_MPI_COMM_REMOTE_SIZE

//MPI_Comm_set_attr
#ifdef USE_MPI_COMM_SET_ATTR
int
MPI_Comm_set_attr (MPI_Comm comm, int comm_keyval, void *attribute_val){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_set_attr_p.shim_id = GMPI_COMM_SET_ATTR;
	shim.MPI_Comm_set_attr_p.comm = comm;
	shim.MPI_Comm_set_attr_p.comm_keyval = comm_keyval;
	shim.MPI_Comm_set_attr_p.attribute_val = attribute_val;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_set_attr_p.rc = PMPI_Comm_set_attr(comm, comm_keyval, attribute_val);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_set_attr_p.rc;
}
#endif //USE_MPI_COMM_SET_ATTR

//MPI_Comm_set_errhandler
#ifdef USE_MPI_COMM_SET_ERRHANDLER
int
MPI_Comm_set_errhandler (MPI_Comm comm, MPI_Errhandler errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_set_errhandler_p.shim_id = GMPI_COMM_SET_ERRHANDLER;
	shim.MPI_Comm_set_errhandler_p.comm = comm;
	shim.MPI_Comm_set_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_set_errhandler_p.rc = PMPI_Comm_set_errhandler(comm, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_set_errhandler_p.rc;
}
#endif //USE_MPI_COMM_SET_ERRHANDLER

//MPI_Comm_set_name
#ifdef USE_MPI_COMM_SET_NAME
int
MPI_Comm_set_name (MPI_Comm comm, char *comm_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_set_name_p.shim_id = GMPI_COMM_SET_NAME;
	shim.MPI_Comm_set_name_p.comm = comm;
	shim.MPI_Comm_set_name_p.comm_name = comm_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_set_name_p.rc = PMPI_Comm_set_name(comm, comm_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_set_name_p.rc;
}
#endif //USE_MPI_COMM_SET_NAME

//MPI_Comm_size
#ifdef USE_MPI_COMM_SIZE
int
MPI_Comm_size (MPI_Comm comm, int *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_size_p.shim_id = GMPI_COMM_SIZE;
	shim.MPI_Comm_size_p.comm = comm;
	shim.MPI_Comm_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_size_p.rc = PMPI_Comm_size(comm, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_size_p.rc;
}
#endif //USE_MPI_COMM_SIZE

//MPI_Comm_spawn
#ifdef USE_MPI_COMM_SPAWN
int
MPI_Comm_spawn (char *command, char **argv, int maxprocs, MPI_Info info, int root, MPI_Comm comm, MPI_Comm *intercomm, int *array_of_errcodes){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_spawn_p.shim_id = GMPI_COMM_SPAWN;
	shim.MPI_Comm_spawn_p.command = command;
	shim.MPI_Comm_spawn_p.argv = argv;
	shim.MPI_Comm_spawn_p.maxprocs = maxprocs;
	shim.MPI_Comm_spawn_p.info = info;
	shim.MPI_Comm_spawn_p.root = root;
	shim.MPI_Comm_spawn_p.comm = comm;
	shim.MPI_Comm_spawn_p.intercomm = intercomm;
	shim.MPI_Comm_spawn_p.array_of_errcodes = array_of_errcodes;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_spawn_p.rc = PMPI_Comm_spawn(command, argv, maxprocs, info, root, comm, intercomm, array_of_errcodes);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_spawn_p.rc;
}
#endif //USE_MPI_COMM_SPAWN

//MPI_Comm_spawn_multiple
#ifdef USE_MPI_COMM_SPAWN_MULTIPLE
int
MPI_Comm_spawn_multiple (int count, char **array_of_commands, char ***array_of_argv, int *array_of_maxprocs, MPI_Info *array_of_info, int root, MPI_Comm comm, MPI_Comm *intercomm, int *array_of_errcodes){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_spawn_multiple_p.shim_id = GMPI_COMM_SPAWN_MULTIPLE;
	shim.MPI_Comm_spawn_multiple_p.count = count;
	shim.MPI_Comm_spawn_multiple_p.array_of_commands = array_of_commands;
	shim.MPI_Comm_spawn_multiple_p.array_of_argv = array_of_argv;
	shim.MPI_Comm_spawn_multiple_p.array_of_maxprocs = array_of_maxprocs;
	shim.MPI_Comm_spawn_multiple_p.array_of_info = array_of_info;
	shim.MPI_Comm_spawn_multiple_p.root = root;
	shim.MPI_Comm_spawn_multiple_p.comm = comm;
	shim.MPI_Comm_spawn_multiple_p.intercomm = intercomm;
	shim.MPI_Comm_spawn_multiple_p.array_of_errcodes = array_of_errcodes;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_spawn_multiple_p.rc = PMPI_Comm_spawn_multiple(count, array_of_commands, array_of_argv, array_of_maxprocs, array_of_info, root, comm, intercomm, array_of_errcodes);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_spawn_multiple_p.rc;
}
#endif //USE_MPI_COMM_SPAWN_MULTIPLE

//MPI_Comm_split
#ifdef USE_MPI_COMM_SPLIT
int
MPI_Comm_split (MPI_Comm comm, int color, int key, MPI_Comm *newcomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_split_p.shim_id = GMPI_COMM_SPLIT;
	shim.MPI_Comm_split_p.comm = comm;
	shim.MPI_Comm_split_p.color = color;
	shim.MPI_Comm_split_p.key = key;
	shim.MPI_Comm_split_p.newcomm = newcomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_split_p.rc = PMPI_Comm_split(comm, color, key, newcomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_split_p.rc;
}
#endif //USE_MPI_COMM_SPLIT

//MPI_Comm_test_inter
#ifdef USE_MPI_COMM_TEST_INTER
int
MPI_Comm_test_inter (MPI_Comm comm, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Comm_test_inter_p.shim_id = GMPI_COMM_TEST_INTER;
	shim.MPI_Comm_test_inter_p.comm = comm;
	shim.MPI_Comm_test_inter_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Comm_test_inter_p.rc = PMPI_Comm_test_inter(comm, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Comm_test_inter_p.rc;
}
#endif //USE_MPI_COMM_TEST_INTER

//MPI_Dims_create
#ifdef USE_MPI_DIMS_CREATE
int
MPI_Dims_create (int nnodes, int ndims, int *dims){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Dims_create_p.shim_id = GMPI_DIMS_CREATE;
	shim.MPI_Dims_create_p.nnodes = nnodes;
	shim.MPI_Dims_create_p.ndims = ndims;
	shim.MPI_Dims_create_p.dims = dims;
	shim_pre( shim_id, &shim );
	shim.MPI_Dims_create_p.rc = PMPI_Dims_create(nnodes, ndims, dims);
	shim_post( shim_id, &shim );
	return shim.MPI_Dims_create_p.rc;
}
#endif //USE_MPI_DIMS_CREATE

//MPI_Errhandler_c2f
#ifdef USE_MPI_ERRHANDLER_C2F
int
MPI_Errhandler_c2f (MPI_Errhandler errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Errhandler_c2f_p.shim_id = GMPI_ERRHANDLER_C2F;
	shim.MPI_Errhandler_c2f_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Errhandler_c2f_p.rc = PMPI_Errhandler_c2f(errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Errhandler_c2f_p.rc;
}
#endif //USE_MPI_ERRHANDLER_C2F

//MPI_Errhandler_create
#ifdef USE_MPI_ERRHANDLER_CREATE
int
MPI_Errhandler_create (MPI_Handler_function *function, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Errhandler_create_p.shim_id = GMPI_ERRHANDLER_CREATE;
	shim.MPI_Errhandler_create_p.function = function;
	shim.MPI_Errhandler_create_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Errhandler_create_p.rc = PMPI_Errhandler_create(function, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Errhandler_create_p.rc;
}
#endif //USE_MPI_ERRHANDLER_CREATE

//MPI_Errhandler_f2c
#ifdef USE_MPI_ERRHANDLER_F2C
MPI_Errhandler
MPI_Errhandler_f2c (int errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Errhandler_f2c_p.shim_id = GMPI_ERRHANDLER_F2C;
	shim.MPI_Errhandler_f2c_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Errhandler_f2c_p.rc = PMPI_Errhandler_f2c(errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Errhandler_f2c_p.rc;
}
#endif //USE_MPI_ERRHANDLER_F2C

//MPI_Errhandler_free
#ifdef USE_MPI_ERRHANDLER_FREE
int
MPI_Errhandler_free (MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Errhandler_free_p.shim_id = GMPI_ERRHANDLER_FREE;
	shim.MPI_Errhandler_free_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Errhandler_free_p.rc = PMPI_Errhandler_free(errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Errhandler_free_p.rc;
}
#endif //USE_MPI_ERRHANDLER_FREE

//MPI_Errhandler_get
#ifdef USE_MPI_ERRHANDLER_GET
int
MPI_Errhandler_get (MPI_Comm comm, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Errhandler_get_p.shim_id = GMPI_ERRHANDLER_GET;
	shim.MPI_Errhandler_get_p.comm = comm;
	shim.MPI_Errhandler_get_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Errhandler_get_p.rc = PMPI_Errhandler_get(comm, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Errhandler_get_p.rc;
}
#endif //USE_MPI_ERRHANDLER_GET

//MPI_Errhandler_set
#ifdef USE_MPI_ERRHANDLER_SET
int
MPI_Errhandler_set (MPI_Comm comm, MPI_Errhandler errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Errhandler_set_p.shim_id = GMPI_ERRHANDLER_SET;
	shim.MPI_Errhandler_set_p.comm = comm;
	shim.MPI_Errhandler_set_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Errhandler_set_p.rc = PMPI_Errhandler_set(comm, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Errhandler_set_p.rc;
}
#endif //USE_MPI_ERRHANDLER_SET

//MPI_Error_class
#ifdef USE_MPI_ERROR_CLASS
int
MPI_Error_class (int errorcode, int *errorclass){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Error_class_p.shim_id = GMPI_ERROR_CLASS;
	shim.MPI_Error_class_p.errorcode = errorcode;
	shim.MPI_Error_class_p.errorclass = errorclass;
	shim_pre( shim_id, &shim );
	shim.MPI_Error_class_p.rc = PMPI_Error_class(errorcode, errorclass);
	shim_post( shim_id, &shim );
	return shim.MPI_Error_class_p.rc;
}
#endif //USE_MPI_ERROR_CLASS

//MPI_Error_string
#ifdef USE_MPI_ERROR_STRING
int
MPI_Error_string (int errorcode, char *string, int *resultlen){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Error_string_p.shim_id = GMPI_ERROR_STRING;
	shim.MPI_Error_string_p.errorcode = errorcode;
	shim.MPI_Error_string_p.string = string;
	shim.MPI_Error_string_p.resultlen = resultlen;
	shim_pre( shim_id, &shim );
	shim.MPI_Error_string_p.rc = PMPI_Error_string(errorcode, string, resultlen);
	shim_post( shim_id, &shim );
	return shim.MPI_Error_string_p.rc;
}
#endif //USE_MPI_ERROR_STRING

//MPI_Exscan
#ifdef USE_MPI_EXSCAN
int
MPI_Exscan (void *sendbuf, void *recvbuf, int count, MPI_Datatype datatype, MPI_Op op, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Exscan_p.shim_id = GMPI_EXSCAN;
	shim.MPI_Exscan_p.sendbuf = sendbuf;
	shim.MPI_Exscan_p.recvbuf = recvbuf;
	shim.MPI_Exscan_p.count = count;
	shim.MPI_Exscan_p.datatype = datatype;
	shim.MPI_Exscan_p.op = op;
	shim.MPI_Exscan_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Exscan_p.rc = PMPI_Exscan(sendbuf, recvbuf, count, datatype, op, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Exscan_p.rc;
}
#endif //USE_MPI_EXSCAN

//MPI_File_c2f
#ifdef USE_MPI_FILE_C2F
int
MPI_File_c2f (MPI_File file){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_c2f_p.shim_id = GMPI_FILE_C2F;
	shim.MPI_File_c2f_p.file = file;
	shim_pre( shim_id, &shim );
	shim.MPI_File_c2f_p.rc = PMPI_File_c2f(file);
	shim_post( shim_id, &shim );
	return shim.MPI_File_c2f_p.rc;
}
#endif //USE_MPI_FILE_C2F

//MPI_File_f2c
#ifdef USE_MPI_FILE_F2C
MPI_File
MPI_File_f2c (int file){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_f2c_p.shim_id = GMPI_FILE_F2C;
	shim.MPI_File_f2c_p.file = file;
	shim_pre( shim_id, &shim );
	shim.MPI_File_f2c_p.rc = PMPI_File_f2c(file);
	shim_post( shim_id, &shim );
	return shim.MPI_File_f2c_p.rc;
}
#endif //USE_MPI_FILE_F2C

//MPI_File_call_errhandler
#ifdef USE_MPI_FILE_CALL_ERRHANDLER
int
MPI_File_call_errhandler (MPI_File fh, int errorcode){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_call_errhandler_p.shim_id = GMPI_FILE_CALL_ERRHANDLER;
	shim.MPI_File_call_errhandler_p.fh = fh;
	shim.MPI_File_call_errhandler_p.errorcode = errorcode;
	shim_pre( shim_id, &shim );
	shim.MPI_File_call_errhandler_p.rc = PMPI_File_call_errhandler(fh, errorcode);
	shim_post( shim_id, &shim );
	return shim.MPI_File_call_errhandler_p.rc;
}
#endif //USE_MPI_FILE_CALL_ERRHANDLER

//MPI_File_create_errhandler
#ifdef USE_MPI_FILE_CREATE_ERRHANDLER
int
MPI_File_create_errhandler (MPI_File_errhandler_fn *function, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_create_errhandler_p.shim_id = GMPI_FILE_CREATE_ERRHANDLER;
	shim.MPI_File_create_errhandler_p.function = function;
	shim.MPI_File_create_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_File_create_errhandler_p.rc = PMPI_File_create_errhandler(function, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_File_create_errhandler_p.rc;
}
#endif //USE_MPI_FILE_CREATE_ERRHANDLER

//MPI_File_set_errhandler
#ifdef USE_MPI_FILE_SET_ERRHANDLER
int
MPI_File_set_errhandler ( MPI_File file, MPI_Errhandler errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_set_errhandler_p.shim_id = GMPI_FILE_SET_ERRHANDLER;
	shim.MPI_File_set_errhandler_p.file = file;
	shim.MPI_File_set_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_File_set_errhandler_p.rc = PMPI_File_set_errhandler(file, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_File_set_errhandler_p.rc;
}
#endif //USE_MPI_FILE_SET_ERRHANDLER

//MPI_File_get_errhandler
#ifdef USE_MPI_FILE_GET_ERRHANDLER
int
MPI_File_get_errhandler ( MPI_File file, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_errhandler_p.shim_id = GMPI_FILE_GET_ERRHANDLER;
	shim.MPI_File_get_errhandler_p.file = file;
	shim.MPI_File_get_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_errhandler_p.rc = PMPI_File_get_errhandler(file, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_errhandler_p.rc;
}
#endif //USE_MPI_FILE_GET_ERRHANDLER

//MPI_File_open
#ifdef USE_MPI_FILE_OPEN
int
MPI_File_open (MPI_Comm comm, char *filename, int amode, MPI_Info info, MPI_File *fh){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_open_p.shim_id = GMPI_FILE_OPEN;
	shim.MPI_File_open_p.comm = comm;
	shim.MPI_File_open_p.filename = filename;
	shim.MPI_File_open_p.amode = amode;
	shim.MPI_File_open_p.info = info;
	shim.MPI_File_open_p.fh = fh;
	shim_pre( shim_id, &shim );
	shim.MPI_File_open_p.rc = PMPI_File_open(comm, filename, amode, info, fh);
	shim_post( shim_id, &shim );
	return shim.MPI_File_open_p.rc;
}
#endif //USE_MPI_FILE_OPEN

//MPI_File_close
#ifdef USE_MPI_FILE_CLOSE
int
MPI_File_close (MPI_File *fh){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_close_p.shim_id = GMPI_FILE_CLOSE;
	shim.MPI_File_close_p.fh = fh;
	shim_pre( shim_id, &shim );
	shim.MPI_File_close_p.rc = PMPI_File_close(fh);
	shim_post( shim_id, &shim );
	return shim.MPI_File_close_p.rc;
}
#endif //USE_MPI_FILE_CLOSE

//MPI_File_delete
#ifdef USE_MPI_FILE_DELETE
int
MPI_File_delete (char *filename, MPI_Info info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_delete_p.shim_id = GMPI_FILE_DELETE;
	shim.MPI_File_delete_p.filename = filename;
	shim.MPI_File_delete_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_File_delete_p.rc = PMPI_File_delete(filename, info);
	shim_post( shim_id, &shim );
	return shim.MPI_File_delete_p.rc;
}
#endif //USE_MPI_FILE_DELETE

//MPI_File_set_size
#ifdef USE_MPI_FILE_SET_SIZE
int
MPI_File_set_size (MPI_File fh, MPI_Offset size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_set_size_p.shim_id = GMPI_FILE_SET_SIZE;
	shim.MPI_File_set_size_p.fh = fh;
	shim.MPI_File_set_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_File_set_size_p.rc = PMPI_File_set_size(fh, size);
	shim_post( shim_id, &shim );
	return shim.MPI_File_set_size_p.rc;
}
#endif //USE_MPI_FILE_SET_SIZE

//MPI_File_preallocate
#ifdef USE_MPI_FILE_PREALLOCATE
int
MPI_File_preallocate (MPI_File fh, MPI_Offset size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_preallocate_p.shim_id = GMPI_FILE_PREALLOCATE;
	shim.MPI_File_preallocate_p.fh = fh;
	shim.MPI_File_preallocate_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_File_preallocate_p.rc = PMPI_File_preallocate(fh, size);
	shim_post( shim_id, &shim );
	return shim.MPI_File_preallocate_p.rc;
}
#endif //USE_MPI_FILE_PREALLOCATE

//MPI_File_get_size
#ifdef USE_MPI_FILE_GET_SIZE
int
MPI_File_get_size (MPI_File fh, MPI_Offset *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_size_p.shim_id = GMPI_FILE_GET_SIZE;
	shim.MPI_File_get_size_p.fh = fh;
	shim.MPI_File_get_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_size_p.rc = PMPI_File_get_size(fh, size);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_size_p.rc;
}
#endif //USE_MPI_FILE_GET_SIZE

//MPI_File_get_group
#ifdef USE_MPI_FILE_GET_GROUP
int
MPI_File_get_group (MPI_File fh, MPI_Group *group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_group_p.shim_id = GMPI_FILE_GET_GROUP;
	shim.MPI_File_get_group_p.fh = fh;
	shim.MPI_File_get_group_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_group_p.rc = PMPI_File_get_group(fh, group);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_group_p.rc;
}
#endif //USE_MPI_FILE_GET_GROUP

//MPI_File_get_amode
#ifdef USE_MPI_FILE_GET_AMODE
int
MPI_File_get_amode (MPI_File fh, int *amode){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_amode_p.shim_id = GMPI_FILE_GET_AMODE;
	shim.MPI_File_get_amode_p.fh = fh;
	shim.MPI_File_get_amode_p.amode = amode;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_amode_p.rc = PMPI_File_get_amode(fh, amode);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_amode_p.rc;
}
#endif //USE_MPI_FILE_GET_AMODE

//MPI_File_set_info
#ifdef USE_MPI_FILE_SET_INFO
int
MPI_File_set_info (MPI_File fh, MPI_Info info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_set_info_p.shim_id = GMPI_FILE_SET_INFO;
	shim.MPI_File_set_info_p.fh = fh;
	shim.MPI_File_set_info_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_File_set_info_p.rc = PMPI_File_set_info(fh, info);
	shim_post( shim_id, &shim );
	return shim.MPI_File_set_info_p.rc;
}
#endif //USE_MPI_FILE_SET_INFO

//MPI_File_get_info
#ifdef USE_MPI_FILE_GET_INFO
int
MPI_File_get_info (MPI_File fh, MPI_Info *info_used){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_info_p.shim_id = GMPI_FILE_GET_INFO;
	shim.MPI_File_get_info_p.fh = fh;
	shim.MPI_File_get_info_p.info_used = info_used;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_info_p.rc = PMPI_File_get_info(fh, info_used);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_info_p.rc;
}
#endif //USE_MPI_FILE_GET_INFO

//MPI_File_set_view
#ifdef USE_MPI_FILE_SET_VIEW
int
MPI_File_set_view (MPI_File fh, MPI_Offset disp, MPI_Datatype etype, MPI_Datatype filetype, char *datarep, MPI_Info info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_set_view_p.shim_id = GMPI_FILE_SET_VIEW;
	shim.MPI_File_set_view_p.fh = fh;
	shim.MPI_File_set_view_p.disp = disp;
	shim.MPI_File_set_view_p.etype = etype;
	shim.MPI_File_set_view_p.filetype = filetype;
	shim.MPI_File_set_view_p.datarep = datarep;
	shim.MPI_File_set_view_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_File_set_view_p.rc = PMPI_File_set_view(fh, disp, etype, filetype, datarep, info);
	shim_post( shim_id, &shim );
	return shim.MPI_File_set_view_p.rc;
}
#endif //USE_MPI_FILE_SET_VIEW

//MPI_File_get_view
#ifdef USE_MPI_FILE_GET_VIEW
int
MPI_File_get_view (MPI_File fh, MPI_Offset *disp, MPI_Datatype *etype, MPI_Datatype *filetype, char *datarep){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_view_p.shim_id = GMPI_FILE_GET_VIEW;
	shim.MPI_File_get_view_p.fh = fh;
	shim.MPI_File_get_view_p.disp = disp;
	shim.MPI_File_get_view_p.etype = etype;
	shim.MPI_File_get_view_p.filetype = filetype;
	shim.MPI_File_get_view_p.datarep = datarep;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_view_p.rc = PMPI_File_get_view(fh, disp, etype, filetype, datarep);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_view_p.rc;
}
#endif //USE_MPI_FILE_GET_VIEW

//MPI_File_read_at
#ifdef USE_MPI_FILE_READ_AT
int
MPI_File_read_at (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_at_p.shim_id = GMPI_FILE_READ_AT;
	shim.MPI_File_read_at_p.fh = fh;
	shim.MPI_File_read_at_p.offset = offset;
	shim.MPI_File_read_at_p.buf = buf;
	shim.MPI_File_read_at_p.count = count;
	shim.MPI_File_read_at_p.datatype = datatype;
	shim.MPI_File_read_at_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_at_p.rc = PMPI_File_read_at(fh, offset, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_at_p.rc;
}
#endif //USE_MPI_FILE_READ_AT

//MPI_File_read_at_all
#ifdef USE_MPI_FILE_READ_AT_ALL
int
MPI_File_read_at_all (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_at_all_p.shim_id = GMPI_FILE_READ_AT_ALL;
	shim.MPI_File_read_at_all_p.fh = fh;
	shim.MPI_File_read_at_all_p.offset = offset;
	shim.MPI_File_read_at_all_p.buf = buf;
	shim.MPI_File_read_at_all_p.count = count;
	shim.MPI_File_read_at_all_p.datatype = datatype;
	shim.MPI_File_read_at_all_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_at_all_p.rc = PMPI_File_read_at_all(fh, offset, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_at_all_p.rc;
}
#endif //USE_MPI_FILE_READ_AT_ALL

//MPI_File_write_at
#ifdef USE_MPI_FILE_WRITE_AT
int
MPI_File_write_at (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_at_p.shim_id = GMPI_FILE_WRITE_AT;
	shim.MPI_File_write_at_p.fh = fh;
	shim.MPI_File_write_at_p.offset = offset;
	shim.MPI_File_write_at_p.buf = buf;
	shim.MPI_File_write_at_p.count = count;
	shim.MPI_File_write_at_p.datatype = datatype;
	shim.MPI_File_write_at_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_at_p.rc = PMPI_File_write_at(fh, offset, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_at_p.rc;
}
#endif //USE_MPI_FILE_WRITE_AT

//MPI_File_write_at_all
#ifdef USE_MPI_FILE_WRITE_AT_ALL
int
MPI_File_write_at_all (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_at_all_p.shim_id = GMPI_FILE_WRITE_AT_ALL;
	shim.MPI_File_write_at_all_p.fh = fh;
	shim.MPI_File_write_at_all_p.offset = offset;
	shim.MPI_File_write_at_all_p.buf = buf;
	shim.MPI_File_write_at_all_p.count = count;
	shim.MPI_File_write_at_all_p.datatype = datatype;
	shim.MPI_File_write_at_all_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_at_all_p.rc = PMPI_File_write_at_all(fh, offset, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_at_all_p.rc;
}
#endif //USE_MPI_FILE_WRITE_AT_ALL

//MPI_File_iread_at
#ifdef USE_MPI_FILE_IREAD_AT
int
MPI_File_iread_at (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_iread_at_p.shim_id = GMPI_FILE_IREAD_AT;
	shim.MPI_File_iread_at_p.fh = fh;
	shim.MPI_File_iread_at_p.offset = offset;
	shim.MPI_File_iread_at_p.buf = buf;
	shim.MPI_File_iread_at_p.count = count;
	shim.MPI_File_iread_at_p.datatype = datatype;
	shim.MPI_File_iread_at_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_File_iread_at_p.rc = PMPI_File_iread_at(fh, offset, buf, count, datatype, request);
	shim_post( shim_id, &shim );
	return shim.MPI_File_iread_at_p.rc;
}
#endif //USE_MPI_FILE_IREAD_AT

//MPI_File_iwrite_at
#ifdef USE_MPI_FILE_IWRITE_AT
int
MPI_File_iwrite_at (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_iwrite_at_p.shim_id = GMPI_FILE_IWRITE_AT;
	shim.MPI_File_iwrite_at_p.fh = fh;
	shim.MPI_File_iwrite_at_p.offset = offset;
	shim.MPI_File_iwrite_at_p.buf = buf;
	shim.MPI_File_iwrite_at_p.count = count;
	shim.MPI_File_iwrite_at_p.datatype = datatype;
	shim.MPI_File_iwrite_at_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_File_iwrite_at_p.rc = PMPI_File_iwrite_at(fh, offset, buf, count, datatype, request);
	shim_post( shim_id, &shim );
	return shim.MPI_File_iwrite_at_p.rc;
}
#endif //USE_MPI_FILE_IWRITE_AT

//MPI_File_read
#ifdef USE_MPI_FILE_READ
int
MPI_File_read (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_p.shim_id = GMPI_FILE_READ;
	shim.MPI_File_read_p.fh = fh;
	shim.MPI_File_read_p.buf = buf;
	shim.MPI_File_read_p.count = count;
	shim.MPI_File_read_p.datatype = datatype;
	shim.MPI_File_read_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_p.rc = PMPI_File_read(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_p.rc;
}
#endif //USE_MPI_FILE_READ

//MPI_File_read_all
#ifdef USE_MPI_FILE_READ_ALL
int
MPI_File_read_all (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_all_p.shim_id = GMPI_FILE_READ_ALL;
	shim.MPI_File_read_all_p.fh = fh;
	shim.MPI_File_read_all_p.buf = buf;
	shim.MPI_File_read_all_p.count = count;
	shim.MPI_File_read_all_p.datatype = datatype;
	shim.MPI_File_read_all_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_all_p.rc = PMPI_File_read_all(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_all_p.rc;
}
#endif //USE_MPI_FILE_READ_ALL

//MPI_File_write
#ifdef USE_MPI_FILE_WRITE
int
MPI_File_write (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_p.shim_id = GMPI_FILE_WRITE;
	shim.MPI_File_write_p.fh = fh;
	shim.MPI_File_write_p.buf = buf;
	shim.MPI_File_write_p.count = count;
	shim.MPI_File_write_p.datatype = datatype;
	shim.MPI_File_write_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_p.rc = PMPI_File_write(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_p.rc;
}
#endif //USE_MPI_FILE_WRITE

//MPI_File_write_all
#ifdef USE_MPI_FILE_WRITE_ALL
int
MPI_File_write_all (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_all_p.shim_id = GMPI_FILE_WRITE_ALL;
	shim.MPI_File_write_all_p.fh = fh;
	shim.MPI_File_write_all_p.buf = buf;
	shim.MPI_File_write_all_p.count = count;
	shim.MPI_File_write_all_p.datatype = datatype;
	shim.MPI_File_write_all_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_all_p.rc = PMPI_File_write_all(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_all_p.rc;
}
#endif //USE_MPI_FILE_WRITE_ALL

//MPI_File_iread
#ifdef USE_MPI_FILE_IREAD
int
MPI_File_iread (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_iread_p.shim_id = GMPI_FILE_IREAD;
	shim.MPI_File_iread_p.fh = fh;
	shim.MPI_File_iread_p.buf = buf;
	shim.MPI_File_iread_p.count = count;
	shim.MPI_File_iread_p.datatype = datatype;
	shim.MPI_File_iread_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_File_iread_p.rc = PMPI_File_iread(fh, buf, count, datatype, request);
	shim_post( shim_id, &shim );
	return shim.MPI_File_iread_p.rc;
}
#endif //USE_MPI_FILE_IREAD

//MPI_File_iwrite
#ifdef USE_MPI_FILE_IWRITE
int
MPI_File_iwrite (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_iwrite_p.shim_id = GMPI_FILE_IWRITE;
	shim.MPI_File_iwrite_p.fh = fh;
	shim.MPI_File_iwrite_p.buf = buf;
	shim.MPI_File_iwrite_p.count = count;
	shim.MPI_File_iwrite_p.datatype = datatype;
	shim.MPI_File_iwrite_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_File_iwrite_p.rc = PMPI_File_iwrite(fh, buf, count, datatype, request);
	shim_post( shim_id, &shim );
	return shim.MPI_File_iwrite_p.rc;
}
#endif //USE_MPI_FILE_IWRITE

//MPI_File_seek
#ifdef USE_MPI_FILE_SEEK
int
MPI_File_seek (MPI_File fh, MPI_Offset offset, int whence){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_seek_p.shim_id = GMPI_FILE_SEEK;
	shim.MPI_File_seek_p.fh = fh;
	shim.MPI_File_seek_p.offset = offset;
	shim.MPI_File_seek_p.whence = whence;
	shim_pre( shim_id, &shim );
	shim.MPI_File_seek_p.rc = PMPI_File_seek(fh, offset, whence);
	shim_post( shim_id, &shim );
	return shim.MPI_File_seek_p.rc;
}
#endif //USE_MPI_FILE_SEEK

//MPI_File_get_position
#ifdef USE_MPI_FILE_GET_POSITION
int
MPI_File_get_position (MPI_File fh, MPI_Offset *offset){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_position_p.shim_id = GMPI_FILE_GET_POSITION;
	shim.MPI_File_get_position_p.fh = fh;
	shim.MPI_File_get_position_p.offset = offset;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_position_p.rc = PMPI_File_get_position(fh, offset);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_position_p.rc;
}
#endif //USE_MPI_FILE_GET_POSITION

//MPI_File_get_byte_offset
#ifdef USE_MPI_FILE_GET_BYTE_OFFSET
int
MPI_File_get_byte_offset (MPI_File fh, MPI_Offset offset, MPI_Offset *disp){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_byte_offset_p.shim_id = GMPI_FILE_GET_BYTE_OFFSET;
	shim.MPI_File_get_byte_offset_p.fh = fh;
	shim.MPI_File_get_byte_offset_p.offset = offset;
	shim.MPI_File_get_byte_offset_p.disp = disp;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_byte_offset_p.rc = PMPI_File_get_byte_offset(fh, offset, disp);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_byte_offset_p.rc;
}
#endif //USE_MPI_FILE_GET_BYTE_OFFSET

//MPI_File_read_shared
#ifdef USE_MPI_FILE_READ_SHARED
int
MPI_File_read_shared (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_shared_p.shim_id = GMPI_FILE_READ_SHARED;
	shim.MPI_File_read_shared_p.fh = fh;
	shim.MPI_File_read_shared_p.buf = buf;
	shim.MPI_File_read_shared_p.count = count;
	shim.MPI_File_read_shared_p.datatype = datatype;
	shim.MPI_File_read_shared_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_shared_p.rc = PMPI_File_read_shared(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_shared_p.rc;
}
#endif //USE_MPI_FILE_READ_SHARED

//MPI_File_write_shared
#ifdef USE_MPI_FILE_WRITE_SHARED
int
MPI_File_write_shared (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_shared_p.shim_id = GMPI_FILE_WRITE_SHARED;
	shim.MPI_File_write_shared_p.fh = fh;
	shim.MPI_File_write_shared_p.buf = buf;
	shim.MPI_File_write_shared_p.count = count;
	shim.MPI_File_write_shared_p.datatype = datatype;
	shim.MPI_File_write_shared_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_shared_p.rc = PMPI_File_write_shared(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_shared_p.rc;
}
#endif //USE_MPI_FILE_WRITE_SHARED

//MPI_File_iread_shared
#ifdef USE_MPI_FILE_IREAD_SHARED
int
MPI_File_iread_shared (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_iread_shared_p.shim_id = GMPI_FILE_IREAD_SHARED;
	shim.MPI_File_iread_shared_p.fh = fh;
	shim.MPI_File_iread_shared_p.buf = buf;
	shim.MPI_File_iread_shared_p.count = count;
	shim.MPI_File_iread_shared_p.datatype = datatype;
	shim.MPI_File_iread_shared_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_File_iread_shared_p.rc = PMPI_File_iread_shared(fh, buf, count, datatype, request);
	shim_post( shim_id, &shim );
	return shim.MPI_File_iread_shared_p.rc;
}
#endif //USE_MPI_FILE_IREAD_SHARED

//MPI_File_iwrite_shared
#ifdef USE_MPI_FILE_IWRITE_SHARED
int
MPI_File_iwrite_shared (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_iwrite_shared_p.shim_id = GMPI_FILE_IWRITE_SHARED;
	shim.MPI_File_iwrite_shared_p.fh = fh;
	shim.MPI_File_iwrite_shared_p.buf = buf;
	shim.MPI_File_iwrite_shared_p.count = count;
	shim.MPI_File_iwrite_shared_p.datatype = datatype;
	shim.MPI_File_iwrite_shared_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_File_iwrite_shared_p.rc = PMPI_File_iwrite_shared(fh, buf, count, datatype, request);
	shim_post( shim_id, &shim );
	return shim.MPI_File_iwrite_shared_p.rc;
}
#endif //USE_MPI_FILE_IWRITE_SHARED

//MPI_File_read_ordered
#ifdef USE_MPI_FILE_READ_ORDERED
int
MPI_File_read_ordered (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_ordered_p.shim_id = GMPI_FILE_READ_ORDERED;
	shim.MPI_File_read_ordered_p.fh = fh;
	shim.MPI_File_read_ordered_p.buf = buf;
	shim.MPI_File_read_ordered_p.count = count;
	shim.MPI_File_read_ordered_p.datatype = datatype;
	shim.MPI_File_read_ordered_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_ordered_p.rc = PMPI_File_read_ordered(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_ordered_p.rc;
}
#endif //USE_MPI_FILE_READ_ORDERED

//MPI_File_write_ordered
#ifdef USE_MPI_FILE_WRITE_ORDERED
int
MPI_File_write_ordered (MPI_File fh, void *buf, int count, MPI_Datatype datatype, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_ordered_p.shim_id = GMPI_FILE_WRITE_ORDERED;
	shim.MPI_File_write_ordered_p.fh = fh;
	shim.MPI_File_write_ordered_p.buf = buf;
	shim.MPI_File_write_ordered_p.count = count;
	shim.MPI_File_write_ordered_p.datatype = datatype;
	shim.MPI_File_write_ordered_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_ordered_p.rc = PMPI_File_write_ordered(fh, buf, count, datatype, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_ordered_p.rc;
}
#endif //USE_MPI_FILE_WRITE_ORDERED

//MPI_File_seek_shared
#ifdef USE_MPI_FILE_SEEK_SHARED
int
MPI_File_seek_shared (MPI_File fh, MPI_Offset offset, int whence){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_seek_shared_p.shim_id = GMPI_FILE_SEEK_SHARED;
	shim.MPI_File_seek_shared_p.fh = fh;
	shim.MPI_File_seek_shared_p.offset = offset;
	shim.MPI_File_seek_shared_p.whence = whence;
	shim_pre( shim_id, &shim );
	shim.MPI_File_seek_shared_p.rc = PMPI_File_seek_shared(fh, offset, whence);
	shim_post( shim_id, &shim );
	return shim.MPI_File_seek_shared_p.rc;
}
#endif //USE_MPI_FILE_SEEK_SHARED

//MPI_File_get_position_shared
#ifdef USE_MPI_FILE_GET_POSITION_SHARED
int
MPI_File_get_position_shared (MPI_File fh, MPI_Offset *offset){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_position_shared_p.shim_id = GMPI_FILE_GET_POSITION_SHARED;
	shim.MPI_File_get_position_shared_p.fh = fh;
	shim.MPI_File_get_position_shared_p.offset = offset;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_position_shared_p.rc = PMPI_File_get_position_shared(fh, offset);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_position_shared_p.rc;
}
#endif //USE_MPI_FILE_GET_POSITION_SHARED

//MPI_File_read_at_all_begin
#ifdef USE_MPI_FILE_READ_AT_ALL_BEGIN
int
MPI_File_read_at_all_begin (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_at_all_begin_p.shim_id = GMPI_FILE_READ_AT_ALL_BEGIN;
	shim.MPI_File_read_at_all_begin_p.fh = fh;
	shim.MPI_File_read_at_all_begin_p.offset = offset;
	shim.MPI_File_read_at_all_begin_p.buf = buf;
	shim.MPI_File_read_at_all_begin_p.count = count;
	shim.MPI_File_read_at_all_begin_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_at_all_begin_p.rc = PMPI_File_read_at_all_begin(fh, offset, buf, count, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_at_all_begin_p.rc;
}
#endif //USE_MPI_FILE_READ_AT_ALL_BEGIN

//MPI_File_read_at_all_end
#ifdef USE_MPI_FILE_READ_AT_ALL_END
int
MPI_File_read_at_all_end (MPI_File fh, void *buf, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_at_all_end_p.shim_id = GMPI_FILE_READ_AT_ALL_END;
	shim.MPI_File_read_at_all_end_p.fh = fh;
	shim.MPI_File_read_at_all_end_p.buf = buf;
	shim.MPI_File_read_at_all_end_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_at_all_end_p.rc = PMPI_File_read_at_all_end(fh, buf, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_at_all_end_p.rc;
}
#endif //USE_MPI_FILE_READ_AT_ALL_END

//MPI_File_write_at_all_begin
#ifdef USE_MPI_FILE_WRITE_AT_ALL_BEGIN
int
MPI_File_write_at_all_begin (MPI_File fh, MPI_Offset offset, void *buf, int count, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_at_all_begin_p.shim_id = GMPI_FILE_WRITE_AT_ALL_BEGIN;
	shim.MPI_File_write_at_all_begin_p.fh = fh;
	shim.MPI_File_write_at_all_begin_p.offset = offset;
	shim.MPI_File_write_at_all_begin_p.buf = buf;
	shim.MPI_File_write_at_all_begin_p.count = count;
	shim.MPI_File_write_at_all_begin_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_at_all_begin_p.rc = PMPI_File_write_at_all_begin(fh, offset, buf, count, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_at_all_begin_p.rc;
}
#endif //USE_MPI_FILE_WRITE_AT_ALL_BEGIN

//MPI_File_write_at_all_end
#ifdef USE_MPI_FILE_WRITE_AT_ALL_END
int
MPI_File_write_at_all_end (MPI_File fh, void *buf, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_at_all_end_p.shim_id = GMPI_FILE_WRITE_AT_ALL_END;
	shim.MPI_File_write_at_all_end_p.fh = fh;
	shim.MPI_File_write_at_all_end_p.buf = buf;
	shim.MPI_File_write_at_all_end_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_at_all_end_p.rc = PMPI_File_write_at_all_end(fh, buf, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_at_all_end_p.rc;
}
#endif //USE_MPI_FILE_WRITE_AT_ALL_END

//MPI_File_read_all_begin
#ifdef USE_MPI_FILE_READ_ALL_BEGIN
int
MPI_File_read_all_begin (MPI_File fh, void *buf, int count, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_all_begin_p.shim_id = GMPI_FILE_READ_ALL_BEGIN;
	shim.MPI_File_read_all_begin_p.fh = fh;
	shim.MPI_File_read_all_begin_p.buf = buf;
	shim.MPI_File_read_all_begin_p.count = count;
	shim.MPI_File_read_all_begin_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_all_begin_p.rc = PMPI_File_read_all_begin(fh, buf, count, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_all_begin_p.rc;
}
#endif //USE_MPI_FILE_READ_ALL_BEGIN

//MPI_File_read_all_end
#ifdef USE_MPI_FILE_READ_ALL_END
int
MPI_File_read_all_end (MPI_File fh, void *buf, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_all_end_p.shim_id = GMPI_FILE_READ_ALL_END;
	shim.MPI_File_read_all_end_p.fh = fh;
	shim.MPI_File_read_all_end_p.buf = buf;
	shim.MPI_File_read_all_end_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_all_end_p.rc = PMPI_File_read_all_end(fh, buf, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_all_end_p.rc;
}
#endif //USE_MPI_FILE_READ_ALL_END

//MPI_File_write_all_begin
#ifdef USE_MPI_FILE_WRITE_ALL_BEGIN
int
MPI_File_write_all_begin (MPI_File fh, void *buf, int count, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_all_begin_p.shim_id = GMPI_FILE_WRITE_ALL_BEGIN;
	shim.MPI_File_write_all_begin_p.fh = fh;
	shim.MPI_File_write_all_begin_p.buf = buf;
	shim.MPI_File_write_all_begin_p.count = count;
	shim.MPI_File_write_all_begin_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_all_begin_p.rc = PMPI_File_write_all_begin(fh, buf, count, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_all_begin_p.rc;
}
#endif //USE_MPI_FILE_WRITE_ALL_BEGIN

//MPI_File_write_all_end
#ifdef USE_MPI_FILE_WRITE_ALL_END
int
MPI_File_write_all_end (MPI_File fh, void *buf, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_all_end_p.shim_id = GMPI_FILE_WRITE_ALL_END;
	shim.MPI_File_write_all_end_p.fh = fh;
	shim.MPI_File_write_all_end_p.buf = buf;
	shim.MPI_File_write_all_end_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_all_end_p.rc = PMPI_File_write_all_end(fh, buf, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_all_end_p.rc;
}
#endif //USE_MPI_FILE_WRITE_ALL_END

//MPI_File_read_ordered_begin
#ifdef USE_MPI_FILE_READ_ORDERED_BEGIN
int
MPI_File_read_ordered_begin (MPI_File fh, void *buf, int count, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_ordered_begin_p.shim_id = GMPI_FILE_READ_ORDERED_BEGIN;
	shim.MPI_File_read_ordered_begin_p.fh = fh;
	shim.MPI_File_read_ordered_begin_p.buf = buf;
	shim.MPI_File_read_ordered_begin_p.count = count;
	shim.MPI_File_read_ordered_begin_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_ordered_begin_p.rc = PMPI_File_read_ordered_begin(fh, buf, count, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_ordered_begin_p.rc;
}
#endif //USE_MPI_FILE_READ_ORDERED_BEGIN

//MPI_File_read_ordered_end
#ifdef USE_MPI_FILE_READ_ORDERED_END
int
MPI_File_read_ordered_end (MPI_File fh, void *buf, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_read_ordered_end_p.shim_id = GMPI_FILE_READ_ORDERED_END;
	shim.MPI_File_read_ordered_end_p.fh = fh;
	shim.MPI_File_read_ordered_end_p.buf = buf;
	shim.MPI_File_read_ordered_end_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_read_ordered_end_p.rc = PMPI_File_read_ordered_end(fh, buf, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_read_ordered_end_p.rc;
}
#endif //USE_MPI_FILE_READ_ORDERED_END

//MPI_File_write_ordered_begin
#ifdef USE_MPI_FILE_WRITE_ORDERED_BEGIN
int
MPI_File_write_ordered_begin (MPI_File fh, void *buf, int count, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_ordered_begin_p.shim_id = GMPI_FILE_WRITE_ORDERED_BEGIN;
	shim.MPI_File_write_ordered_begin_p.fh = fh;
	shim.MPI_File_write_ordered_begin_p.buf = buf;
	shim.MPI_File_write_ordered_begin_p.count = count;
	shim.MPI_File_write_ordered_begin_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_ordered_begin_p.rc = PMPI_File_write_ordered_begin(fh, buf, count, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_ordered_begin_p.rc;
}
#endif //USE_MPI_FILE_WRITE_ORDERED_BEGIN

//MPI_File_write_ordered_end
#ifdef USE_MPI_FILE_WRITE_ORDERED_END
int
MPI_File_write_ordered_end (MPI_File fh, void *buf, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_write_ordered_end_p.shim_id = GMPI_FILE_WRITE_ORDERED_END;
	shim.MPI_File_write_ordered_end_p.fh = fh;
	shim.MPI_File_write_ordered_end_p.buf = buf;
	shim.MPI_File_write_ordered_end_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_File_write_ordered_end_p.rc = PMPI_File_write_ordered_end(fh, buf, status);
	shim_post( shim_id, &shim );
	return shim.MPI_File_write_ordered_end_p.rc;
}
#endif //USE_MPI_FILE_WRITE_ORDERED_END

//MPI_File_get_type_extent
#ifdef USE_MPI_FILE_GET_TYPE_EXTENT
int
MPI_File_get_type_extent (MPI_File fh, MPI_Datatype datatype, MPI_Aint *extent){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_type_extent_p.shim_id = GMPI_FILE_GET_TYPE_EXTENT;
	shim.MPI_File_get_type_extent_p.fh = fh;
	shim.MPI_File_get_type_extent_p.datatype = datatype;
	shim.MPI_File_get_type_extent_p.extent = extent;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_type_extent_p.rc = PMPI_File_get_type_extent(fh, datatype, extent);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_type_extent_p.rc;
}
#endif //USE_MPI_FILE_GET_TYPE_EXTENT

//MPI_File_set_atomicity
#ifdef USE_MPI_FILE_SET_ATOMICITY
int
MPI_File_set_atomicity (MPI_File fh, int flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_set_atomicity_p.shim_id = GMPI_FILE_SET_ATOMICITY;
	shim.MPI_File_set_atomicity_p.fh = fh;
	shim.MPI_File_set_atomicity_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_File_set_atomicity_p.rc = PMPI_File_set_atomicity(fh, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_File_set_atomicity_p.rc;
}
#endif //USE_MPI_FILE_SET_ATOMICITY

//MPI_File_get_atomicity
#ifdef USE_MPI_FILE_GET_ATOMICITY
int
MPI_File_get_atomicity (MPI_File fh, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_get_atomicity_p.shim_id = GMPI_FILE_GET_ATOMICITY;
	shim.MPI_File_get_atomicity_p.fh = fh;
	shim.MPI_File_get_atomicity_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_File_get_atomicity_p.rc = PMPI_File_get_atomicity(fh, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_File_get_atomicity_p.rc;
}
#endif //USE_MPI_FILE_GET_ATOMICITY

//MPI_File_sync
#ifdef USE_MPI_FILE_SYNC
int
MPI_File_sync (MPI_File fh){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_File_sync_p.shim_id = GMPI_FILE_SYNC;
	shim.MPI_File_sync_p.fh = fh;
	shim_pre( shim_id, &shim );
	shim.MPI_File_sync_p.rc = PMPI_File_sync(fh);
	shim_post( shim_id, &shim );
	return shim.MPI_File_sync_p.rc;
}
#endif //USE_MPI_FILE_SYNC

//MPI_Finalize
#ifdef USE_MPI_FINALIZE
int
MPI_Finalize (void){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Finalize_p.shim_id = GMPI_FINALIZE;
	shim_pre( shim_id, &shim );
	shim.MPI_Finalize_p.rc = PMPI_Finalize();
	shim_post( shim_id, &shim );
	return shim.MPI_Finalize_p.rc;
}
#endif //USE_MPI_FINALIZE

//MPI_Finalized
#ifdef USE_MPI_FINALIZED
int
MPI_Finalized (int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Finalized_p.shim_id = GMPI_FINALIZED;
	shim.MPI_Finalized_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Finalized_p.rc = PMPI_Finalized(flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Finalized_p.rc;
}
#endif //USE_MPI_FINALIZED

//MPI_Free_mem
#ifdef USE_MPI_FREE_MEM
int
MPI_Free_mem (void *base){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Free_mem_p.shim_id = GMPI_FREE_MEM;
	shim.MPI_Free_mem_p.base = base;
	shim_pre( shim_id, &shim );
	shim.MPI_Free_mem_p.rc = PMPI_Free_mem(base);
	shim_post( shim_id, &shim );
	return shim.MPI_Free_mem_p.rc;
}
#endif //USE_MPI_FREE_MEM

//MPI_Gather
#ifdef USE_MPI_GATHER
int
MPI_Gather (void *sendbuf, int sendcount, MPI_Datatype sendtype, void *recvbuf, int recvcount, MPI_Datatype recvtype, int root, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Gather_p.shim_id = GMPI_GATHER;
	shim.MPI_Gather_p.sendbuf = sendbuf;
	shim.MPI_Gather_p.sendcount = sendcount;
	shim.MPI_Gather_p.sendtype = sendtype;
	shim.MPI_Gather_p.recvbuf = recvbuf;
	shim.MPI_Gather_p.recvcount = recvcount;
	shim.MPI_Gather_p.recvtype = recvtype;
	shim.MPI_Gather_p.root = root;
	shim.MPI_Gather_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Gather_p.rc = PMPI_Gather(sendbuf, sendcount, sendtype, recvbuf, recvcount, recvtype, root, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Gather_p.rc;
}
#endif //USE_MPI_GATHER

//MPI_Gatherv
#ifdef USE_MPI_GATHERV
int
MPI_Gatherv (void *sendbuf, int sendcount, MPI_Datatype sendtype, void *recvbuf, int *recvcounts, int *displs, MPI_Datatype recvtype, int root, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Gatherv_p.shim_id = GMPI_GATHERV;
	shim.MPI_Gatherv_p.sendbuf = sendbuf;
	shim.MPI_Gatherv_p.sendcount = sendcount;
	shim.MPI_Gatherv_p.sendtype = sendtype;
	shim.MPI_Gatherv_p.recvbuf = recvbuf;
	shim.MPI_Gatherv_p.recvcounts = recvcounts;
	shim.MPI_Gatherv_p.displs = displs;
	shim.MPI_Gatherv_p.recvtype = recvtype;
	shim.MPI_Gatherv_p.root = root;
	shim.MPI_Gatherv_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Gatherv_p.rc = PMPI_Gatherv(sendbuf, sendcount, sendtype, recvbuf, recvcounts, displs, recvtype, root, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Gatherv_p.rc;
}
#endif //USE_MPI_GATHERV

//MPI_Get_address
#ifdef USE_MPI_GET_ADDRESS
int
MPI_Get_address (void *location, MPI_Aint *address){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Get_address_p.shim_id = GMPI_GET_ADDRESS;
	shim.MPI_Get_address_p.location = location;
	shim.MPI_Get_address_p.address = address;
	shim_pre( shim_id, &shim );
	shim.MPI_Get_address_p.rc = PMPI_Get_address(location, address);
	shim_post( shim_id, &shim );
	return shim.MPI_Get_address_p.rc;
}
#endif //USE_MPI_GET_ADDRESS

//MPI_Get_count
#ifdef USE_MPI_GET_COUNT
int
MPI_Get_count (MPI_Status *status, MPI_Datatype datatype, int *count){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Get_count_p.shim_id = GMPI_GET_COUNT;
	shim.MPI_Get_count_p.status = status;
	shim.MPI_Get_count_p.datatype = datatype;
	shim.MPI_Get_count_p.count = count;
	shim_pre( shim_id, &shim );
	shim.MPI_Get_count_p.rc = PMPI_Get_count(status, datatype, count);
	shim_post( shim_id, &shim );
	return shim.MPI_Get_count_p.rc;
}
#endif //USE_MPI_GET_COUNT

//MPI_Get_elements
#ifdef USE_MPI_GET_ELEMENTS
int
MPI_Get_elements (MPI_Status *status, MPI_Datatype datatype, int *count){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Get_elements_p.shim_id = GMPI_GET_ELEMENTS;
	shim.MPI_Get_elements_p.status = status;
	shim.MPI_Get_elements_p.datatype = datatype;
	shim.MPI_Get_elements_p.count = count;
	shim_pre( shim_id, &shim );
	shim.MPI_Get_elements_p.rc = PMPI_Get_elements(status, datatype, count);
	shim_post( shim_id, &shim );
	return shim.MPI_Get_elements_p.rc;
}
#endif //USE_MPI_GET_ELEMENTS

//MPI_Get
#ifdef USE_MPI_GET
int
MPI_Get (void *origin_addr, int origin_count, MPI_Datatype origin_datatype, int target_rank, MPI_Aint target_disp, int target_count, MPI_Datatype target_datatype, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Get_p.shim_id = GMPI_GET;
	shim.MPI_Get_p.origin_addr = origin_addr;
	shim.MPI_Get_p.origin_count = origin_count;
	shim.MPI_Get_p.origin_datatype = origin_datatype;
	shim.MPI_Get_p.target_rank = target_rank;
	shim.MPI_Get_p.target_disp = target_disp;
	shim.MPI_Get_p.target_count = target_count;
	shim.MPI_Get_p.target_datatype = target_datatype;
	shim.MPI_Get_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Get_p.rc = PMPI_Get(origin_addr, origin_count, origin_datatype, target_rank, target_disp, target_count, target_datatype, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Get_p.rc;
}
#endif //USE_MPI_GET

//MPI_Get_processor_name
#ifdef USE_MPI_GET_PROCESSOR_NAME
int
MPI_Get_processor_name (char *name, int *resultlen){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Get_processor_name_p.shim_id = GMPI_GET_PROCESSOR_NAME;
	shim.MPI_Get_processor_name_p.name = name;
	shim.MPI_Get_processor_name_p.resultlen = resultlen;
	shim_pre( shim_id, &shim );
	shim.MPI_Get_processor_name_p.rc = PMPI_Get_processor_name(name, resultlen);
	shim_post( shim_id, &shim );
	return shim.MPI_Get_processor_name_p.rc;
}
#endif //USE_MPI_GET_PROCESSOR_NAME

//MPI_Get_version
#ifdef USE_MPI_GET_VERSION
int
MPI_Get_version (int *version, int *subversion){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Get_version_p.shim_id = GMPI_GET_VERSION;
	shim.MPI_Get_version_p.version = version;
	shim.MPI_Get_version_p.subversion = subversion;
	shim_pre( shim_id, &shim );
	shim.MPI_Get_version_p.rc = PMPI_Get_version(version, subversion);
	shim_post( shim_id, &shim );
	return shim.MPI_Get_version_p.rc;
}
#endif //USE_MPI_GET_VERSION

//MPI_Graph_create
#ifdef USE_MPI_GRAPH_CREATE
int
MPI_Graph_create (MPI_Comm comm_old, int nnodes, int *index, int *edges, int reorder, MPI_Comm *comm_graph){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Graph_create_p.shim_id = GMPI_GRAPH_CREATE;
	shim.MPI_Graph_create_p.comm_old = comm_old;
	shim.MPI_Graph_create_p.nnodes = nnodes;
	shim.MPI_Graph_create_p.index = index;
	shim.MPI_Graph_create_p.edges = edges;
	shim.MPI_Graph_create_p.reorder = reorder;
	shim.MPI_Graph_create_p.comm_graph = comm_graph;
	shim_pre( shim_id, &shim );
	shim.MPI_Graph_create_p.rc = PMPI_Graph_create(comm_old, nnodes, index, edges, reorder, comm_graph);
	shim_post( shim_id, &shim );
	return shim.MPI_Graph_create_p.rc;
}
#endif //USE_MPI_GRAPH_CREATE

//MPI_Graph_get
#ifdef USE_MPI_GRAPH_GET
int
MPI_Graph_get (MPI_Comm comm, int maxindex, int maxedges, int *index, int *edges){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Graph_get_p.shim_id = GMPI_GRAPH_GET;
	shim.MPI_Graph_get_p.comm = comm;
	shim.MPI_Graph_get_p.maxindex = maxindex;
	shim.MPI_Graph_get_p.maxedges = maxedges;
	shim.MPI_Graph_get_p.index = index;
	shim.MPI_Graph_get_p.edges = edges;
	shim_pre( shim_id, &shim );
	shim.MPI_Graph_get_p.rc = PMPI_Graph_get(comm, maxindex, maxedges, index, edges);
	shim_post( shim_id, &shim );
	return shim.MPI_Graph_get_p.rc;
}
#endif //USE_MPI_GRAPH_GET

//MPI_Graph_map
#ifdef USE_MPI_GRAPH_MAP
int
MPI_Graph_map (MPI_Comm comm, int nnodes, int *index, int *edges, int *newrank){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Graph_map_p.shim_id = GMPI_GRAPH_MAP;
	shim.MPI_Graph_map_p.comm = comm;
	shim.MPI_Graph_map_p.nnodes = nnodes;
	shim.MPI_Graph_map_p.index = index;
	shim.MPI_Graph_map_p.edges = edges;
	shim.MPI_Graph_map_p.newrank = newrank;
	shim_pre( shim_id, &shim );
	shim.MPI_Graph_map_p.rc = PMPI_Graph_map(comm, nnodes, index, edges, newrank);
	shim_post( shim_id, &shim );
	return shim.MPI_Graph_map_p.rc;
}
#endif //USE_MPI_GRAPH_MAP

//MPI_Graph_neighbors_count
#ifdef USE_MPI_GRAPH_NEIGHBORS_COUNT
int
MPI_Graph_neighbors_count (MPI_Comm comm, int rank, int *nneighbors){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Graph_neighbors_count_p.shim_id = GMPI_GRAPH_NEIGHBORS_COUNT;
	shim.MPI_Graph_neighbors_count_p.comm = comm;
	shim.MPI_Graph_neighbors_count_p.rank = rank;
	shim.MPI_Graph_neighbors_count_p.nneighbors = nneighbors;
	shim_pre( shim_id, &shim );
	shim.MPI_Graph_neighbors_count_p.rc = PMPI_Graph_neighbors_count(comm, rank, nneighbors);
	shim_post( shim_id, &shim );
	return shim.MPI_Graph_neighbors_count_p.rc;
}
#endif //USE_MPI_GRAPH_NEIGHBORS_COUNT

//MPI_Graph_neighbors
#ifdef USE_MPI_GRAPH_NEIGHBORS
int
MPI_Graph_neighbors (MPI_Comm comm, int rank, int maxneighbors, int *neighbors){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Graph_neighbors_p.shim_id = GMPI_GRAPH_NEIGHBORS;
	shim.MPI_Graph_neighbors_p.comm = comm;
	shim.MPI_Graph_neighbors_p.rank = rank;
	shim.MPI_Graph_neighbors_p.maxneighbors = maxneighbors;
	shim.MPI_Graph_neighbors_p.neighbors = neighbors;
	shim_pre( shim_id, &shim );
	shim.MPI_Graph_neighbors_p.rc = PMPI_Graph_neighbors(comm, rank, maxneighbors, neighbors);
	shim_post( shim_id, &shim );
	return shim.MPI_Graph_neighbors_p.rc;
}
#endif //USE_MPI_GRAPH_NEIGHBORS

//MPI_Graphdims_get
#ifdef USE_MPI_GRAPHDIMS_GET
int
MPI_Graphdims_get (MPI_Comm comm, int *nnodes, int *nedges){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Graphdims_get_p.shim_id = GMPI_GRAPHDIMS_GET;
	shim.MPI_Graphdims_get_p.comm = comm;
	shim.MPI_Graphdims_get_p.nnodes = nnodes;
	shim.MPI_Graphdims_get_p.nedges = nedges;
	shim_pre( shim_id, &shim );
	shim.MPI_Graphdims_get_p.rc = PMPI_Graphdims_get(comm, nnodes, nedges);
	shim_post( shim_id, &shim );
	return shim.MPI_Graphdims_get_p.rc;
}
#endif //USE_MPI_GRAPHDIMS_GET

//MPI_Grequest_complete
#ifdef USE_MPI_GREQUEST_COMPLETE
int
MPI_Grequest_complete (MPI_Request request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Grequest_complete_p.shim_id = GMPI_GREQUEST_COMPLETE;
	shim.MPI_Grequest_complete_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Grequest_complete_p.rc = PMPI_Grequest_complete(request);
	shim_post( shim_id, &shim );
	return shim.MPI_Grequest_complete_p.rc;
}
#endif //USE_MPI_GREQUEST_COMPLETE

//MPI_Grequest_start
#ifdef USE_MPI_GREQUEST_START
int
MPI_Grequest_start (MPI_Grequest_query_function *query_fn, MPI_Grequest_free_function *free_fn, MPI_Grequest_cancel_function *cancel_fn, void *extra_state, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Grequest_start_p.shim_id = GMPI_GREQUEST_START;
	shim.MPI_Grequest_start_p.query_fn = query_fn;
	shim.MPI_Grequest_start_p.free_fn = free_fn;
	shim.MPI_Grequest_start_p.cancel_fn = cancel_fn;
	shim.MPI_Grequest_start_p.extra_state = extra_state;
	shim.MPI_Grequest_start_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Grequest_start_p.rc = PMPI_Grequest_start(query_fn, free_fn, cancel_fn, extra_state, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Grequest_start_p.rc;
}
#endif //USE_MPI_GREQUEST_START

//MPI_Group_c2f
#ifdef USE_MPI_GROUP_C2F
int
MPI_Group_c2f (MPI_Group group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_c2f_p.shim_id = GMPI_GROUP_C2F;
	shim.MPI_Group_c2f_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_c2f_p.rc = PMPI_Group_c2f(group);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_c2f_p.rc;
}
#endif //USE_MPI_GROUP_C2F

//MPI_Group_compare
#ifdef USE_MPI_GROUP_COMPARE
int
MPI_Group_compare (MPI_Group group1, MPI_Group group2, int *result){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_compare_p.shim_id = GMPI_GROUP_COMPARE;
	shim.MPI_Group_compare_p.group1 = group1;
	shim.MPI_Group_compare_p.group2 = group2;
	shim.MPI_Group_compare_p.result = result;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_compare_p.rc = PMPI_Group_compare(group1, group2, result);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_compare_p.rc;
}
#endif //USE_MPI_GROUP_COMPARE

//MPI_Group_difference
#ifdef USE_MPI_GROUP_DIFFERENCE
int
MPI_Group_difference (MPI_Group group1, MPI_Group group2, MPI_Group *newgroup){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_difference_p.shim_id = GMPI_GROUP_DIFFERENCE;
	shim.MPI_Group_difference_p.group1 = group1;
	shim.MPI_Group_difference_p.group2 = group2;
	shim.MPI_Group_difference_p.newgroup = newgroup;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_difference_p.rc = PMPI_Group_difference(group1, group2, newgroup);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_difference_p.rc;
}
#endif //USE_MPI_GROUP_DIFFERENCE

//MPI_Group_excl
#ifdef USE_MPI_GROUP_EXCL
int
MPI_Group_excl (MPI_Group group, int n, int *ranks, MPI_Group *newgroup){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_excl_p.shim_id = GMPI_GROUP_EXCL;
	shim.MPI_Group_excl_p.group = group;
	shim.MPI_Group_excl_p.n = n;
	shim.MPI_Group_excl_p.ranks = ranks;
	shim.MPI_Group_excl_p.newgroup = newgroup;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_excl_p.rc = PMPI_Group_excl(group, n, ranks, newgroup);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_excl_p.rc;
}
#endif //USE_MPI_GROUP_EXCL

//MPI_Group_f2c
#ifdef USE_MPI_GROUP_F2C
MPI_Group
MPI_Group_f2c (int group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_f2c_p.shim_id = GMPI_GROUP_F2C;
	shim.MPI_Group_f2c_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_f2c_p.rc = PMPI_Group_f2c(group);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_f2c_p.rc;
}
#endif //USE_MPI_GROUP_F2C

//MPI_Group_free
#ifdef USE_MPI_GROUP_FREE
int
MPI_Group_free (MPI_Group *group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_free_p.shim_id = GMPI_GROUP_FREE;
	shim.MPI_Group_free_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_free_p.rc = PMPI_Group_free(group);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_free_p.rc;
}
#endif //USE_MPI_GROUP_FREE

//MPI_Group_incl
#ifdef USE_MPI_GROUP_INCL
int
MPI_Group_incl (MPI_Group group, int n, int *ranks, MPI_Group *newgroup){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_incl_p.shim_id = GMPI_GROUP_INCL;
	shim.MPI_Group_incl_p.group = group;
	shim.MPI_Group_incl_p.n = n;
	shim.MPI_Group_incl_p.ranks = ranks;
	shim.MPI_Group_incl_p.newgroup = newgroup;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_incl_p.rc = PMPI_Group_incl(group, n, ranks, newgroup);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_incl_p.rc;
}
#endif //USE_MPI_GROUP_INCL

//MPI_Group_intersection
#ifdef USE_MPI_GROUP_INTERSECTION
int
MPI_Group_intersection (MPI_Group group1, MPI_Group group2, MPI_Group *newgroup){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_intersection_p.shim_id = GMPI_GROUP_INTERSECTION;
	shim.MPI_Group_intersection_p.group1 = group1;
	shim.MPI_Group_intersection_p.group2 = group2;
	shim.MPI_Group_intersection_p.newgroup = newgroup;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_intersection_p.rc = PMPI_Group_intersection(group1, group2, newgroup);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_intersection_p.rc;
}
#endif //USE_MPI_GROUP_INTERSECTION

//MPI_Group_rank
#ifdef USE_MPI_GROUP_RANK
int
MPI_Group_rank (MPI_Group group, int *rank){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_rank_p.shim_id = GMPI_GROUP_RANK;
	shim.MPI_Group_rank_p.group = group;
	shim.MPI_Group_rank_p.rank = rank;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_rank_p.rc = PMPI_Group_rank(group, rank);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_rank_p.rc;
}
#endif //USE_MPI_GROUP_RANK

//MPI_Group_size
#ifdef USE_MPI_GROUP_SIZE
int
MPI_Group_size (MPI_Group group, int *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_size_p.shim_id = GMPI_GROUP_SIZE;
	shim.MPI_Group_size_p.group = group;
	shim.MPI_Group_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_size_p.rc = PMPI_Group_size(group, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_size_p.rc;
}
#endif //USE_MPI_GROUP_SIZE

//MPI_Group_translate_ranks
#ifdef USE_MPI_GROUP_TRANSLATE_RANKS
int
MPI_Group_translate_ranks (MPI_Group group1, int n, int *ranks1, MPI_Group group2, int *ranks2){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_translate_ranks_p.shim_id = GMPI_GROUP_TRANSLATE_RANKS;
	shim.MPI_Group_translate_ranks_p.group1 = group1;
	shim.MPI_Group_translate_ranks_p.n = n;
	shim.MPI_Group_translate_ranks_p.ranks1 = ranks1;
	shim.MPI_Group_translate_ranks_p.group2 = group2;
	shim.MPI_Group_translate_ranks_p.ranks2 = ranks2;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_translate_ranks_p.rc = PMPI_Group_translate_ranks(group1, n, ranks1, group2, ranks2);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_translate_ranks_p.rc;
}
#endif //USE_MPI_GROUP_TRANSLATE_RANKS

//MPI_Group_union
#ifdef USE_MPI_GROUP_UNION
int
MPI_Group_union (MPI_Group group1, MPI_Group group2, MPI_Group *newgroup){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Group_union_p.shim_id = GMPI_GROUP_UNION;
	shim.MPI_Group_union_p.group1 = group1;
	shim.MPI_Group_union_p.group2 = group2;
	shim.MPI_Group_union_p.newgroup = newgroup;
	shim_pre( shim_id, &shim );
	shim.MPI_Group_union_p.rc = PMPI_Group_union(group1, group2, newgroup);
	shim_post( shim_id, &shim );
	return shim.MPI_Group_union_p.rc;
}
#endif //USE_MPI_GROUP_UNION

//MPI_Ibsend
#ifdef USE_MPI_IBSEND
int
MPI_Ibsend (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Ibsend_p.shim_id = GMPI_IBSEND;
	shim.MPI_Ibsend_p.buf = buf;
	shim.MPI_Ibsend_p.count = count;
	shim.MPI_Ibsend_p.datatype = datatype;
	shim.MPI_Ibsend_p.dest = dest;
	shim.MPI_Ibsend_p.tag = tag;
	shim.MPI_Ibsend_p.comm = comm;
	shim.MPI_Ibsend_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Ibsend_p.rc = PMPI_Ibsend(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Ibsend_p.rc;
}
#endif //USE_MPI_IBSEND

//MPI_Info_c2f
#ifdef USE_MPI_INFO_C2F
int
MPI_Info_c2f (MPI_Info info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_c2f_p.shim_id = GMPI_INFO_C2F;
	shim.MPI_Info_c2f_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_c2f_p.rc = PMPI_Info_c2f(info);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_c2f_p.rc;
}
#endif //USE_MPI_INFO_C2F

//MPI_Info_create
#ifdef USE_MPI_INFO_CREATE
int
MPI_Info_create (MPI_Info *info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_create_p.shim_id = GMPI_INFO_CREATE;
	shim.MPI_Info_create_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_create_p.rc = PMPI_Info_create(info);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_create_p.rc;
}
#endif //USE_MPI_INFO_CREATE

//MPI_Info_delete
#ifdef USE_MPI_INFO_DELETE
int
MPI_Info_delete (MPI_Info info, char *key){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_delete_p.shim_id = GMPI_INFO_DELETE;
	shim.MPI_Info_delete_p.info = info;
	shim.MPI_Info_delete_p.key = key;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_delete_p.rc = PMPI_Info_delete(info, key);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_delete_p.rc;
}
#endif //USE_MPI_INFO_DELETE

//MPI_Info_dup
#ifdef USE_MPI_INFO_DUP
int
MPI_Info_dup (MPI_Info info, MPI_Info *newinfo){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_dup_p.shim_id = GMPI_INFO_DUP;
	shim.MPI_Info_dup_p.info = info;
	shim.MPI_Info_dup_p.newinfo = newinfo;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_dup_p.rc = PMPI_Info_dup(info, newinfo);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_dup_p.rc;
}
#endif //USE_MPI_INFO_DUP

//MPI_Info_f2c
#ifdef USE_MPI_INFO_F2C
MPI_Info
MPI_Info_f2c (int info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_f2c_p.shim_id = GMPI_INFO_F2C;
	shim.MPI_Info_f2c_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_f2c_p.rc = PMPI_Info_f2c(info);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_f2c_p.rc;
}
#endif //USE_MPI_INFO_F2C

//MPI_Info_free
#ifdef USE_MPI_INFO_FREE
int
MPI_Info_free (MPI_Info *info){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_free_p.shim_id = GMPI_INFO_FREE;
	shim.MPI_Info_free_p.info = info;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_free_p.rc = PMPI_Info_free(info);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_free_p.rc;
}
#endif //USE_MPI_INFO_FREE

//MPI_Info_get
#ifdef USE_MPI_INFO_GET
int
MPI_Info_get (MPI_Info info, char *key, int valuelen, char *value, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_get_p.shim_id = GMPI_INFO_GET;
	shim.MPI_Info_get_p.info = info;
	shim.MPI_Info_get_p.key = key;
	shim.MPI_Info_get_p.valuelen = valuelen;
	shim.MPI_Info_get_p.value = value;
	shim.MPI_Info_get_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_get_p.rc = PMPI_Info_get(info, key, valuelen, value, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_get_p.rc;
}
#endif //USE_MPI_INFO_GET

//MPI_Info_get_nkeys
#ifdef USE_MPI_INFO_GET_NKEYS
int
MPI_Info_get_nkeys (MPI_Info info, int *nkeys){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_get_nkeys_p.shim_id = GMPI_INFO_GET_NKEYS;
	shim.MPI_Info_get_nkeys_p.info = info;
	shim.MPI_Info_get_nkeys_p.nkeys = nkeys;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_get_nkeys_p.rc = PMPI_Info_get_nkeys(info, nkeys);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_get_nkeys_p.rc;
}
#endif //USE_MPI_INFO_GET_NKEYS

//MPI_Info_get_nthkey
#ifdef USE_MPI_INFO_GET_NTHKEY
int
MPI_Info_get_nthkey (MPI_Info info, int n, char *key){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_get_nthkey_p.shim_id = GMPI_INFO_GET_NTHKEY;
	shim.MPI_Info_get_nthkey_p.info = info;
	shim.MPI_Info_get_nthkey_p.n = n;
	shim.MPI_Info_get_nthkey_p.key = key;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_get_nthkey_p.rc = PMPI_Info_get_nthkey(info, n, key);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_get_nthkey_p.rc;
}
#endif //USE_MPI_INFO_GET_NTHKEY

//MPI_Info_get_valuelen
#ifdef USE_MPI_INFO_GET_VALUELEN
int
MPI_Info_get_valuelen (MPI_Info info, char *key, int *valuelen, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_get_valuelen_p.shim_id = GMPI_INFO_GET_VALUELEN;
	shim.MPI_Info_get_valuelen_p.info = info;
	shim.MPI_Info_get_valuelen_p.key = key;
	shim.MPI_Info_get_valuelen_p.valuelen = valuelen;
	shim.MPI_Info_get_valuelen_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_get_valuelen_p.rc = PMPI_Info_get_valuelen(info, key, valuelen, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_get_valuelen_p.rc;
}
#endif //USE_MPI_INFO_GET_VALUELEN

//MPI_Info_set
#ifdef USE_MPI_INFO_SET
int
MPI_Info_set (MPI_Info info, char *key, char *value){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Info_set_p.shim_id = GMPI_INFO_SET;
	shim.MPI_Info_set_p.info = info;
	shim.MPI_Info_set_p.key = key;
	shim.MPI_Info_set_p.value = value;
	shim_pre( shim_id, &shim );
	shim.MPI_Info_set_p.rc = PMPI_Info_set(info, key, value);
	shim_post( shim_id, &shim );
	return shim.MPI_Info_set_p.rc;
}
#endif //USE_MPI_INFO_SET

//MPI_Init
#ifdef USE_MPI_INIT
int
MPI_Init (int *argc, char ***argv){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Init_p.shim_id = GMPI_INIT;
	shim.MPI_Init_p.argc = argc;
	shim.MPI_Init_p.argv = argv;
	shim_pre( shim_id, &shim );
	shim.MPI_Init_p.rc = PMPI_Init(argc, argv);
	shim_post( shim_id, &shim );
	return shim.MPI_Init_p.rc;
}
#endif //USE_MPI_INIT

//MPI_Initialized
#ifdef USE_MPI_INITIALIZED
int
MPI_Initialized (int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Initialized_p.shim_id = GMPI_INITIALIZED;
	shim.MPI_Initialized_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Initialized_p.rc = PMPI_Initialized(flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Initialized_p.rc;
}
#endif //USE_MPI_INITIALIZED

//MPI_Init_thread
#ifdef USE_MPI_INIT_THREAD
int
MPI_Init_thread (int *argc, char ***argv, int required, int *provided){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Init_thread_p.shim_id = GMPI_INIT_THREAD;
	shim.MPI_Init_thread_p.argc = argc;
	shim.MPI_Init_thread_p.argv = argv;
	shim.MPI_Init_thread_p.required = required;
	shim.MPI_Init_thread_p.provided = provided;
	shim_pre( shim_id, &shim );
	shim.MPI_Init_thread_p.rc = PMPI_Init_thread(argc, argv, required, provided);
	shim_post( shim_id, &shim );
	return shim.MPI_Init_thread_p.rc;
}
#endif //USE_MPI_INIT_THREAD

//MPI_Intercomm_create
#ifdef USE_MPI_INTERCOMM_CREATE
int
MPI_Intercomm_create (MPI_Comm local_comm, int local_leader, MPI_Comm bridge_comm, int remote_leader, int tag, MPI_Comm *newintercomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Intercomm_create_p.shim_id = GMPI_INTERCOMM_CREATE;
	shim.MPI_Intercomm_create_p.local_comm = local_comm;
	shim.MPI_Intercomm_create_p.local_leader = local_leader;
	shim.MPI_Intercomm_create_p.bridge_comm = bridge_comm;
	shim.MPI_Intercomm_create_p.remote_leader = remote_leader;
	shim.MPI_Intercomm_create_p.tag = tag;
	shim.MPI_Intercomm_create_p.newintercomm = newintercomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Intercomm_create_p.rc = PMPI_Intercomm_create(local_comm, local_leader, bridge_comm, remote_leader, tag, newintercomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Intercomm_create_p.rc;
}
#endif //USE_MPI_INTERCOMM_CREATE

//MPI_Intercomm_merge
#ifdef USE_MPI_INTERCOMM_MERGE
int
MPI_Intercomm_merge (MPI_Comm intercomm, int high, MPI_Comm *newintercomm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Intercomm_merge_p.shim_id = GMPI_INTERCOMM_MERGE;
	shim.MPI_Intercomm_merge_p.intercomm = intercomm;
	shim.MPI_Intercomm_merge_p.high = high;
	shim.MPI_Intercomm_merge_p.newintercomm = newintercomm;
	shim_pre( shim_id, &shim );
	shim.MPI_Intercomm_merge_p.rc = PMPI_Intercomm_merge(intercomm, high, newintercomm);
	shim_post( shim_id, &shim );
	return shim.MPI_Intercomm_merge_p.rc;
}
#endif //USE_MPI_INTERCOMM_MERGE

//MPI_Iprobe
#ifdef USE_MPI_IPROBE
int
MPI_Iprobe (int source, int tag, MPI_Comm comm, int *flag, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Iprobe_p.shim_id = GMPI_IPROBE;
	shim.MPI_Iprobe_p.source = source;
	shim.MPI_Iprobe_p.tag = tag;
	shim.MPI_Iprobe_p.comm = comm;
	shim.MPI_Iprobe_p.flag = flag;
	shim.MPI_Iprobe_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Iprobe_p.rc = PMPI_Iprobe(source, tag, comm, flag, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Iprobe_p.rc;
}
#endif //USE_MPI_IPROBE

//MPI_Irecv
#ifdef USE_MPI_IRECV
int
MPI_Irecv (void *buf, int count, MPI_Datatype datatype, int source, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Irecv_p.shim_id = GMPI_IRECV;
	shim.MPI_Irecv_p.buf = buf;
	shim.MPI_Irecv_p.count = count;
	shim.MPI_Irecv_p.datatype = datatype;
	shim.MPI_Irecv_p.source = source;
	shim.MPI_Irecv_p.tag = tag;
	shim.MPI_Irecv_p.comm = comm;
	shim.MPI_Irecv_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Irecv_p.rc = PMPI_Irecv(buf, count, datatype, source, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Irecv_p.rc;
}
#endif //USE_MPI_IRECV

//MPI_Irsend
#ifdef USE_MPI_IRSEND
int
MPI_Irsend (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Irsend_p.shim_id = GMPI_IRSEND;
	shim.MPI_Irsend_p.buf = buf;
	shim.MPI_Irsend_p.count = count;
	shim.MPI_Irsend_p.datatype = datatype;
	shim.MPI_Irsend_p.dest = dest;
	shim.MPI_Irsend_p.tag = tag;
	shim.MPI_Irsend_p.comm = comm;
	shim.MPI_Irsend_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Irsend_p.rc = PMPI_Irsend(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Irsend_p.rc;
}
#endif //USE_MPI_IRSEND

//MPI_Isend
#ifdef USE_MPI_ISEND
int
MPI_Isend (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Isend_p.shim_id = GMPI_ISEND;
	shim.MPI_Isend_p.buf = buf;
	shim.MPI_Isend_p.count = count;
	shim.MPI_Isend_p.datatype = datatype;
	shim.MPI_Isend_p.dest = dest;
	shim.MPI_Isend_p.tag = tag;
	shim.MPI_Isend_p.comm = comm;
	shim.MPI_Isend_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Isend_p.rc = PMPI_Isend(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Isend_p.rc;
}
#endif //USE_MPI_ISEND

//MPI_Issend
#ifdef USE_MPI_ISSEND
int
MPI_Issend (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Issend_p.shim_id = GMPI_ISSEND;
	shim.MPI_Issend_p.buf = buf;
	shim.MPI_Issend_p.count = count;
	shim.MPI_Issend_p.datatype = datatype;
	shim.MPI_Issend_p.dest = dest;
	shim.MPI_Issend_p.tag = tag;
	shim.MPI_Issend_p.comm = comm;
	shim.MPI_Issend_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Issend_p.rc = PMPI_Issend(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Issend_p.rc;
}
#endif //USE_MPI_ISSEND

//MPI_Is_thread_main
#ifdef USE_MPI_IS_THREAD_MAIN
int
MPI_Is_thread_main (int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Is_thread_main_p.shim_id = GMPI_IS_THREAD_MAIN;
	shim.MPI_Is_thread_main_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Is_thread_main_p.rc = PMPI_Is_thread_main(flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Is_thread_main_p.rc;
}
#endif //USE_MPI_IS_THREAD_MAIN

//MPI_Keyval_create
#ifdef USE_MPI_KEYVAL_CREATE
int
MPI_Keyval_create (MPI_Copy_function *copy_fn, MPI_Delete_function *delete_fn, int *keyval, void *extra_state){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Keyval_create_p.shim_id = GMPI_KEYVAL_CREATE;
	shim.MPI_Keyval_create_p.copy_fn = copy_fn;
	shim.MPI_Keyval_create_p.delete_fn = delete_fn;
	shim.MPI_Keyval_create_p.keyval = keyval;
	shim.MPI_Keyval_create_p.extra_state = extra_state;
	shim_pre( shim_id, &shim );
	shim.MPI_Keyval_create_p.rc = PMPI_Keyval_create(copy_fn, delete_fn, keyval, extra_state);
	shim_post( shim_id, &shim );
	return shim.MPI_Keyval_create_p.rc;
}
#endif //USE_MPI_KEYVAL_CREATE

//MPI_Keyval_free
#ifdef USE_MPI_KEYVAL_FREE
int
MPI_Keyval_free (int *keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Keyval_free_p.shim_id = GMPI_KEYVAL_FREE;
	shim.MPI_Keyval_free_p.keyval = keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Keyval_free_p.rc = PMPI_Keyval_free(keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Keyval_free_p.rc;
}
#endif //USE_MPI_KEYVAL_FREE

//MPI_Lookup_name
#ifdef USE_MPI_LOOKUP_NAME
int
MPI_Lookup_name (char *service_name, MPI_Info info, char *port_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Lookup_name_p.shim_id = GMPI_LOOKUP_NAME;
	shim.MPI_Lookup_name_p.service_name = service_name;
	shim.MPI_Lookup_name_p.info = info;
	shim.MPI_Lookup_name_p.port_name = port_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Lookup_name_p.rc = PMPI_Lookup_name(service_name, info, port_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Lookup_name_p.rc;
}
#endif //USE_MPI_LOOKUP_NAME

//MPI_Op_c2f
#ifdef USE_MPI_OP_C2F
int
MPI_Op_c2f (MPI_Op op){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Op_c2f_p.shim_id = GMPI_OP_C2F;
	shim.MPI_Op_c2f_p.op = op;
	shim_pre( shim_id, &shim );
	shim.MPI_Op_c2f_p.rc = PMPI_Op_c2f(op);
	shim_post( shim_id, &shim );
	return shim.MPI_Op_c2f_p.rc;
}
#endif //USE_MPI_OP_C2F

//MPI_Op_create
#ifdef USE_MPI_OP_CREATE
int
MPI_Op_create (MPI_User_function *function, int commute, MPI_Op *op){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Op_create_p.shim_id = GMPI_OP_CREATE;
	shim.MPI_Op_create_p.function = function;
	shim.MPI_Op_create_p.commute = commute;
	shim.MPI_Op_create_p.op = op;
	shim_pre( shim_id, &shim );
	shim.MPI_Op_create_p.rc = PMPI_Op_create(function, commute, op);
	shim_post( shim_id, &shim );
	return shim.MPI_Op_create_p.rc;
}
#endif //USE_MPI_OP_CREATE

//MPI_Open_port
#ifdef USE_MPI_OPEN_PORT
int
MPI_Open_port (MPI_Info info, char *port_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Open_port_p.shim_id = GMPI_OPEN_PORT;
	shim.MPI_Open_port_p.info = info;
	shim.MPI_Open_port_p.port_name = port_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Open_port_p.rc = PMPI_Open_port(info, port_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Open_port_p.rc;
}
#endif //USE_MPI_OPEN_PORT

//MPI_Op_f2c
#ifdef USE_MPI_OP_F2C
MPI_Op
MPI_Op_f2c (int op){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Op_f2c_p.shim_id = GMPI_OP_F2C;
	shim.MPI_Op_f2c_p.op = op;
	shim_pre( shim_id, &shim );
	shim.MPI_Op_f2c_p.rc = PMPI_Op_f2c(op);
	shim_post( shim_id, &shim );
	return shim.MPI_Op_f2c_p.rc;
}
#endif //USE_MPI_OP_F2C

//MPI_Op_free
#ifdef USE_MPI_OP_FREE
int
MPI_Op_free (MPI_Op *op){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Op_free_p.shim_id = GMPI_OP_FREE;
	shim.MPI_Op_free_p.op = op;
	shim_pre( shim_id, &shim );
	shim.MPI_Op_free_p.rc = PMPI_Op_free(op);
	shim_post( shim_id, &shim );
	return shim.MPI_Op_free_p.rc;
}
#endif //USE_MPI_OP_FREE

//MPI_Pack_external
#ifdef USE_MPI_PACK_EXTERNAL
int
MPI_Pack_external (char *datarep, void *inbuf, int incount, MPI_Datatype datatype, void *outbuf, MPI_Aint outsize, MPI_Aint *position){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Pack_external_p.shim_id = GMPI_PACK_EXTERNAL;
	shim.MPI_Pack_external_p.datarep = datarep;
	shim.MPI_Pack_external_p.inbuf = inbuf;
	shim.MPI_Pack_external_p.incount = incount;
	shim.MPI_Pack_external_p.datatype = datatype;
	shim.MPI_Pack_external_p.outbuf = outbuf;
	shim.MPI_Pack_external_p.outsize = outsize;
	shim.MPI_Pack_external_p.position = position;
	shim_pre( shim_id, &shim );
	shim.MPI_Pack_external_p.rc = PMPI_Pack_external(datarep, inbuf, incount, datatype, outbuf, outsize, position);
	shim_post( shim_id, &shim );
	return shim.MPI_Pack_external_p.rc;
}
#endif //USE_MPI_PACK_EXTERNAL

//MPI_Pack_external_size
#ifdef USE_MPI_PACK_EXTERNAL_SIZE
int
MPI_Pack_external_size (char *datarep, int incount, MPI_Datatype datatype, MPI_Aint *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Pack_external_size_p.shim_id = GMPI_PACK_EXTERNAL_SIZE;
	shim.MPI_Pack_external_size_p.datarep = datarep;
	shim.MPI_Pack_external_size_p.incount = incount;
	shim.MPI_Pack_external_size_p.datatype = datatype;
	shim.MPI_Pack_external_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Pack_external_size_p.rc = PMPI_Pack_external_size(datarep, incount, datatype, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Pack_external_size_p.rc;
}
#endif //USE_MPI_PACK_EXTERNAL_SIZE

//MPI_Pack
#ifdef USE_MPI_PACK
int
MPI_Pack (void *inbuf, int incount, MPI_Datatype datatype, void *outbuf, int outsize, int *position, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Pack_p.shim_id = GMPI_PACK;
	shim.MPI_Pack_p.inbuf = inbuf;
	shim.MPI_Pack_p.incount = incount;
	shim.MPI_Pack_p.datatype = datatype;
	shim.MPI_Pack_p.outbuf = outbuf;
	shim.MPI_Pack_p.outsize = outsize;
	shim.MPI_Pack_p.position = position;
	shim.MPI_Pack_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Pack_p.rc = PMPI_Pack(inbuf, incount, datatype, outbuf, outsize, position, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Pack_p.rc;
}
#endif //USE_MPI_PACK

//MPI_Pack_size
#ifdef USE_MPI_PACK_SIZE
int
MPI_Pack_size (int incount, MPI_Datatype datatype, MPI_Comm comm, int *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Pack_size_p.shim_id = GMPI_PACK_SIZE;
	shim.MPI_Pack_size_p.incount = incount;
	shim.MPI_Pack_size_p.datatype = datatype;
	shim.MPI_Pack_size_p.comm = comm;
	shim.MPI_Pack_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Pack_size_p.rc = PMPI_Pack_size(incount, datatype, comm, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Pack_size_p.rc;
}
#endif //USE_MPI_PACK_SIZE

//MPI_Pcontrol
#ifdef USE_MPI_PCONTROL
int
MPI_Pcontrol (const int level, ...){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Pcontrol_p.shim_id = GMPI_PCONTROL;
	shim.MPI_Pcontrol_p.level = level;
	shim_pre( shim_id, &shim );
	shim.MPI_Pcontrol_p.rc = PMPI_Pcontrol(level);
	shim_post( shim_id, &shim );
	return shim.MPI_Pcontrol_p.rc;
}
#endif //USE_MPI_PCONTROL

//MPI_Probe
#ifdef USE_MPI_PROBE
int
MPI_Probe (int source, int tag, MPI_Comm comm, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Probe_p.shim_id = GMPI_PROBE;
	shim.MPI_Probe_p.source = source;
	shim.MPI_Probe_p.tag = tag;
	shim.MPI_Probe_p.comm = comm;
	shim.MPI_Probe_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Probe_p.rc = PMPI_Probe(source, tag, comm, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Probe_p.rc;
}
#endif //USE_MPI_PROBE

//MPI_Publish_name
#ifdef USE_MPI_PUBLISH_NAME
int
MPI_Publish_name (char *service_name, MPI_Info info, char *port_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Publish_name_p.shim_id = GMPI_PUBLISH_NAME;
	shim.MPI_Publish_name_p.service_name = service_name;
	shim.MPI_Publish_name_p.info = info;
	shim.MPI_Publish_name_p.port_name = port_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Publish_name_p.rc = PMPI_Publish_name(service_name, info, port_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Publish_name_p.rc;
}
#endif //USE_MPI_PUBLISH_NAME

//MPI_Put
#ifdef USE_MPI_PUT
int
MPI_Put (void *origin_addr, int origin_count, MPI_Datatype origin_datatype, int target_rank, MPI_Aint target_disp, int target_count, MPI_Datatype target_datatype, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Put_p.shim_id = GMPI_PUT;
	shim.MPI_Put_p.origin_addr = origin_addr;
	shim.MPI_Put_p.origin_count = origin_count;
	shim.MPI_Put_p.origin_datatype = origin_datatype;
	shim.MPI_Put_p.target_rank = target_rank;
	shim.MPI_Put_p.target_disp = target_disp;
	shim.MPI_Put_p.target_count = target_count;
	shim.MPI_Put_p.target_datatype = target_datatype;
	shim.MPI_Put_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Put_p.rc = PMPI_Put(origin_addr, origin_count, origin_datatype, target_rank, target_disp, target_count, target_datatype, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Put_p.rc;
}
#endif //USE_MPI_PUT

//MPI_Query_thread
#ifdef USE_MPI_QUERY_THREAD
int
MPI_Query_thread (int *provided){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Query_thread_p.shim_id = GMPI_QUERY_THREAD;
	shim.MPI_Query_thread_p.provided = provided;
	shim_pre( shim_id, &shim );
	shim.MPI_Query_thread_p.rc = PMPI_Query_thread(provided);
	shim_post( shim_id, &shim );
	return shim.MPI_Query_thread_p.rc;
}
#endif //USE_MPI_QUERY_THREAD

//MPI_Recv_init
#ifdef USE_MPI_RECV_INIT
int
MPI_Recv_init (void *buf, int count, MPI_Datatype datatype, int source, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Recv_init_p.shim_id = GMPI_RECV_INIT;
	shim.MPI_Recv_init_p.buf = buf;
	shim.MPI_Recv_init_p.count = count;
	shim.MPI_Recv_init_p.datatype = datatype;
	shim.MPI_Recv_init_p.source = source;
	shim.MPI_Recv_init_p.tag = tag;
	shim.MPI_Recv_init_p.comm = comm;
	shim.MPI_Recv_init_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Recv_init_p.rc = PMPI_Recv_init(buf, count, datatype, source, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Recv_init_p.rc;
}
#endif //USE_MPI_RECV_INIT

//MPI_Recv
#ifdef USE_MPI_RECV
int
MPI_Recv (void *buf, int count, MPI_Datatype datatype, int source, int tag, MPI_Comm comm, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Recv_p.shim_id = GMPI_RECV;
	shim.MPI_Recv_p.buf = buf;
	shim.MPI_Recv_p.count = count;
	shim.MPI_Recv_p.datatype = datatype;
	shim.MPI_Recv_p.source = source;
	shim.MPI_Recv_p.tag = tag;
	shim.MPI_Recv_p.comm = comm;
	shim.MPI_Recv_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Recv_p.rc = PMPI_Recv(buf, count, datatype, source, tag, comm, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Recv_p.rc;
}
#endif //USE_MPI_RECV

//MPI_Reduce
#ifdef USE_MPI_REDUCE
int
MPI_Reduce (void *sendbuf, void *recvbuf, int count, MPI_Datatype datatype, MPI_Op op, int root, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Reduce_p.shim_id = GMPI_REDUCE;
	shim.MPI_Reduce_p.sendbuf = sendbuf;
	shim.MPI_Reduce_p.recvbuf = recvbuf;
	shim.MPI_Reduce_p.count = count;
	shim.MPI_Reduce_p.datatype = datatype;
	shim.MPI_Reduce_p.op = op;
	shim.MPI_Reduce_p.root = root;
	shim.MPI_Reduce_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Reduce_p.rc = PMPI_Reduce(sendbuf, recvbuf, count, datatype, op, root, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Reduce_p.rc;
}
#endif //USE_MPI_REDUCE

//MPI_Reduce_scatter
#ifdef USE_MPI_REDUCE_SCATTER
int
MPI_Reduce_scatter (void *sendbuf, void *recvbuf, int *recvcounts, MPI_Datatype datatype, MPI_Op op, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Reduce_scatter_p.shim_id = GMPI_REDUCE_SCATTER;
	shim.MPI_Reduce_scatter_p.sendbuf = sendbuf;
	shim.MPI_Reduce_scatter_p.recvbuf = recvbuf;
	shim.MPI_Reduce_scatter_p.recvcounts = recvcounts;
	shim.MPI_Reduce_scatter_p.datatype = datatype;
	shim.MPI_Reduce_scatter_p.op = op;
	shim.MPI_Reduce_scatter_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Reduce_scatter_p.rc = PMPI_Reduce_scatter(sendbuf, recvbuf, recvcounts, datatype, op, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Reduce_scatter_p.rc;
}
#endif //USE_MPI_REDUCE_SCATTER

//MPI_Register_datarep
#ifdef USE_MPI_REGISTER_DATAREP
int
MPI_Register_datarep (char *datarep, MPI_Datarep_conversion_function *read_conversion_fn, MPI_Datarep_conversion_function *write_conversion_fn, MPI_Datarep_extent_function *dtype_file_extent_fn, void *extra_state){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Register_datarep_p.shim_id = GMPI_REGISTER_DATAREP;
	shim.MPI_Register_datarep_p.datarep = datarep;
	shim.MPI_Register_datarep_p.read_conversion_fn = read_conversion_fn;
	shim.MPI_Register_datarep_p.write_conversion_fn = write_conversion_fn;
	shim.MPI_Register_datarep_p.dtype_file_extent_fn = dtype_file_extent_fn;
	shim.MPI_Register_datarep_p.extra_state = extra_state;
	shim_pre( shim_id, &shim );
	shim.MPI_Register_datarep_p.rc = PMPI_Register_datarep(datarep, read_conversion_fn, write_conversion_fn, dtype_file_extent_fn, extra_state);
	shim_post( shim_id, &shim );
	return shim.MPI_Register_datarep_p.rc;
}
#endif //USE_MPI_REGISTER_DATAREP

//MPI_Request_c2f
#ifdef USE_MPI_REQUEST_C2F
int
MPI_Request_c2f (MPI_Request request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Request_c2f_p.shim_id = GMPI_REQUEST_C2F;
	shim.MPI_Request_c2f_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Request_c2f_p.rc = PMPI_Request_c2f(request);
	shim_post( shim_id, &shim );
	return shim.MPI_Request_c2f_p.rc;
}
#endif //USE_MPI_REQUEST_C2F

//MPI_Request_f2c
#ifdef USE_MPI_REQUEST_F2C
MPI_Request
MPI_Request_f2c (int request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Request_f2c_p.shim_id = GMPI_REQUEST_F2C;
	shim.MPI_Request_f2c_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Request_f2c_p.rc = PMPI_Request_f2c(request);
	shim_post( shim_id, &shim );
	return shim.MPI_Request_f2c_p.rc;
}
#endif //USE_MPI_REQUEST_F2C

//MPI_Request_free
#ifdef USE_MPI_REQUEST_FREE
int
MPI_Request_free (MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Request_free_p.shim_id = GMPI_REQUEST_FREE;
	shim.MPI_Request_free_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Request_free_p.rc = PMPI_Request_free(request);
	shim_post( shim_id, &shim );
	return shim.MPI_Request_free_p.rc;
}
#endif //USE_MPI_REQUEST_FREE

//MPI_Request_get_status
#ifdef USE_MPI_REQUEST_GET_STATUS
int
MPI_Request_get_status (MPI_Request request, int *flag, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Request_get_status_p.shim_id = GMPI_REQUEST_GET_STATUS;
	shim.MPI_Request_get_status_p.request = request;
	shim.MPI_Request_get_status_p.flag = flag;
	shim.MPI_Request_get_status_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Request_get_status_p.rc = PMPI_Request_get_status(request, flag, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Request_get_status_p.rc;
}
#endif //USE_MPI_REQUEST_GET_STATUS

//MPI_Rsend
#ifdef USE_MPI_RSEND
int
MPI_Rsend (void *ibuf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Rsend_p.shim_id = GMPI_RSEND;
	shim.MPI_Rsend_p.ibuf = ibuf;
	shim.MPI_Rsend_p.count = count;
	shim.MPI_Rsend_p.datatype = datatype;
	shim.MPI_Rsend_p.dest = dest;
	shim.MPI_Rsend_p.tag = tag;
	shim.MPI_Rsend_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Rsend_p.rc = PMPI_Rsend(ibuf, count, datatype, dest, tag, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Rsend_p.rc;
}
#endif //USE_MPI_RSEND

//MPI_Rsend_init
#ifdef USE_MPI_RSEND_INIT
int
MPI_Rsend_init (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Rsend_init_p.shim_id = GMPI_RSEND_INIT;
	shim.MPI_Rsend_init_p.buf = buf;
	shim.MPI_Rsend_init_p.count = count;
	shim.MPI_Rsend_init_p.datatype = datatype;
	shim.MPI_Rsend_init_p.dest = dest;
	shim.MPI_Rsend_init_p.tag = tag;
	shim.MPI_Rsend_init_p.comm = comm;
	shim.MPI_Rsend_init_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Rsend_init_p.rc = PMPI_Rsend_init(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Rsend_init_p.rc;
}
#endif //USE_MPI_RSEND_INIT

//MPI_Scan
#ifdef USE_MPI_SCAN
int
MPI_Scan (void *sendbuf, void *recvbuf, int count, MPI_Datatype datatype, MPI_Op op, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Scan_p.shim_id = GMPI_SCAN;
	shim.MPI_Scan_p.sendbuf = sendbuf;
	shim.MPI_Scan_p.recvbuf = recvbuf;
	shim.MPI_Scan_p.count = count;
	shim.MPI_Scan_p.datatype = datatype;
	shim.MPI_Scan_p.op = op;
	shim.MPI_Scan_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Scan_p.rc = PMPI_Scan(sendbuf, recvbuf, count, datatype, op, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Scan_p.rc;
}
#endif //USE_MPI_SCAN

//MPI_Scatter
#ifdef USE_MPI_SCATTER
int
MPI_Scatter (void *sendbuf, int sendcount, MPI_Datatype sendtype, void *recvbuf, int recvcount, MPI_Datatype recvtype, int root, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Scatter_p.shim_id = GMPI_SCATTER;
	shim.MPI_Scatter_p.sendbuf = sendbuf;
	shim.MPI_Scatter_p.sendcount = sendcount;
	shim.MPI_Scatter_p.sendtype = sendtype;
	shim.MPI_Scatter_p.recvbuf = recvbuf;
	shim.MPI_Scatter_p.recvcount = recvcount;
	shim.MPI_Scatter_p.recvtype = recvtype;
	shim.MPI_Scatter_p.root = root;
	shim.MPI_Scatter_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Scatter_p.rc = PMPI_Scatter(sendbuf, sendcount, sendtype, recvbuf, recvcount, recvtype, root, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Scatter_p.rc;
}
#endif //USE_MPI_SCATTER

//MPI_Scatterv
#ifdef USE_MPI_SCATTERV
int
MPI_Scatterv (void *sendbuf, int *sendcounts, int *displs, MPI_Datatype sendtype, void *recvbuf, int recvcount, MPI_Datatype recvtype, int root, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Scatterv_p.shim_id = GMPI_SCATTERV;
	shim.MPI_Scatterv_p.sendbuf = sendbuf;
	shim.MPI_Scatterv_p.sendcounts = sendcounts;
	shim.MPI_Scatterv_p.displs = displs;
	shim.MPI_Scatterv_p.sendtype = sendtype;
	shim.MPI_Scatterv_p.recvbuf = recvbuf;
	shim.MPI_Scatterv_p.recvcount = recvcount;
	shim.MPI_Scatterv_p.recvtype = recvtype;
	shim.MPI_Scatterv_p.root = root;
	shim.MPI_Scatterv_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Scatterv_p.rc = PMPI_Scatterv(sendbuf, sendcounts, displs, sendtype, recvbuf, recvcount, recvtype, root, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Scatterv_p.rc;
}
#endif //USE_MPI_SCATTERV

//MPI_Send_init
#ifdef USE_MPI_SEND_INIT
int
MPI_Send_init (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Send_init_p.shim_id = GMPI_SEND_INIT;
	shim.MPI_Send_init_p.buf = buf;
	shim.MPI_Send_init_p.count = count;
	shim.MPI_Send_init_p.datatype = datatype;
	shim.MPI_Send_init_p.dest = dest;
	shim.MPI_Send_init_p.tag = tag;
	shim.MPI_Send_init_p.comm = comm;
	shim.MPI_Send_init_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Send_init_p.rc = PMPI_Send_init(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Send_init_p.rc;
}
#endif //USE_MPI_SEND_INIT

//MPI_Send
#ifdef USE_MPI_SEND
int
MPI_Send (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Send_p.shim_id = GMPI_SEND;
	shim.MPI_Send_p.buf = buf;
	shim.MPI_Send_p.count = count;
	shim.MPI_Send_p.datatype = datatype;
	shim.MPI_Send_p.dest = dest;
	shim.MPI_Send_p.tag = tag;
	shim.MPI_Send_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Send_p.rc = PMPI_Send(buf, count, datatype, dest, tag, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Send_p.rc;
}
#endif //USE_MPI_SEND

//MPI_Sendrecv
#ifdef USE_MPI_SENDRECV
int
MPI_Sendrecv (void *sendbuf, int sendcount, MPI_Datatype sendtype, int dest, int sendtag, void *recvbuf, int recvcount, MPI_Datatype recvtype, int source, int recvtag, MPI_Comm comm, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Sendrecv_p.shim_id = GMPI_SENDRECV;
	shim.MPI_Sendrecv_p.sendbuf = sendbuf;
	shim.MPI_Sendrecv_p.sendcount = sendcount;
	shim.MPI_Sendrecv_p.sendtype = sendtype;
	shim.MPI_Sendrecv_p.dest = dest;
	shim.MPI_Sendrecv_p.sendtag = sendtag;
	shim.MPI_Sendrecv_p.recvbuf = recvbuf;
	shim.MPI_Sendrecv_p.recvcount = recvcount;
	shim.MPI_Sendrecv_p.recvtype = recvtype;
	shim.MPI_Sendrecv_p.source = source;
	shim.MPI_Sendrecv_p.recvtag = recvtag;
	shim.MPI_Sendrecv_p.comm = comm;
	shim.MPI_Sendrecv_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Sendrecv_p.rc = PMPI_Sendrecv(sendbuf, sendcount, sendtype, dest, sendtag, recvbuf, recvcount, recvtype, source, recvtag, comm, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Sendrecv_p.rc;
}
#endif //USE_MPI_SENDRECV

//MPI_Sendrecv_replace
#ifdef USE_MPI_SENDRECV_REPLACE
int
MPI_Sendrecv_replace (void *buf, int count, MPI_Datatype datatype, int dest, int sendtag, int source, int recvtag, MPI_Comm comm, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Sendrecv_replace_p.shim_id = GMPI_SENDRECV_REPLACE;
	shim.MPI_Sendrecv_replace_p.buf = buf;
	shim.MPI_Sendrecv_replace_p.count = count;
	shim.MPI_Sendrecv_replace_p.datatype = datatype;
	shim.MPI_Sendrecv_replace_p.dest = dest;
	shim.MPI_Sendrecv_replace_p.sendtag = sendtag;
	shim.MPI_Sendrecv_replace_p.source = source;
	shim.MPI_Sendrecv_replace_p.recvtag = recvtag;
	shim.MPI_Sendrecv_replace_p.comm = comm;
	shim.MPI_Sendrecv_replace_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Sendrecv_replace_p.rc = PMPI_Sendrecv_replace(buf, count, datatype, dest, sendtag, source, recvtag, comm, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Sendrecv_replace_p.rc;
}
#endif //USE_MPI_SENDRECV_REPLACE

//MPI_Ssend_init
#ifdef USE_MPI_SSEND_INIT
int
MPI_Ssend_init (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm, MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Ssend_init_p.shim_id = GMPI_SSEND_INIT;
	shim.MPI_Ssend_init_p.buf = buf;
	shim.MPI_Ssend_init_p.count = count;
	shim.MPI_Ssend_init_p.datatype = datatype;
	shim.MPI_Ssend_init_p.dest = dest;
	shim.MPI_Ssend_init_p.tag = tag;
	shim.MPI_Ssend_init_p.comm = comm;
	shim.MPI_Ssend_init_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Ssend_init_p.rc = PMPI_Ssend_init(buf, count, datatype, dest, tag, comm, request);
	shim_post( shim_id, &shim );
	return shim.MPI_Ssend_init_p.rc;
}
#endif //USE_MPI_SSEND_INIT

//MPI_Ssend
#ifdef USE_MPI_SSEND
int
MPI_Ssend (void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Ssend_p.shim_id = GMPI_SSEND;
	shim.MPI_Ssend_p.buf = buf;
	shim.MPI_Ssend_p.count = count;
	shim.MPI_Ssend_p.datatype = datatype;
	shim.MPI_Ssend_p.dest = dest;
	shim.MPI_Ssend_p.tag = tag;
	shim.MPI_Ssend_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Ssend_p.rc = PMPI_Ssend(buf, count, datatype, dest, tag, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Ssend_p.rc;
}
#endif //USE_MPI_SSEND

//MPI_Start
#ifdef USE_MPI_START
int
MPI_Start (MPI_Request *request){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Start_p.shim_id = GMPI_START;
	shim.MPI_Start_p.request = request;
	shim_pre( shim_id, &shim );
	shim.MPI_Start_p.rc = PMPI_Start(request);
	shim_post( shim_id, &shim );
	return shim.MPI_Start_p.rc;
}
#endif //USE_MPI_START

//MPI_Startall
#ifdef USE_MPI_STARTALL
int
MPI_Startall (int count, MPI_Request *array_of_requests){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Startall_p.shim_id = GMPI_STARTALL;
	shim.MPI_Startall_p.count = count;
	shim.MPI_Startall_p.array_of_requests = array_of_requests;
	shim_pre( shim_id, &shim );
	shim.MPI_Startall_p.rc = PMPI_Startall(count, array_of_requests);
	shim_post( shim_id, &shim );
	return shim.MPI_Startall_p.rc;
}
#endif //USE_MPI_STARTALL

//MPI_Status_c2f
#ifdef USE_MPI_STATUS_C2F
int
MPI_Status_c2f (MPI_Status *c_status, int *f_status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Status_c2f_p.shim_id = GMPI_STATUS_C2F;
	shim.MPI_Status_c2f_p.c_status = c_status;
	shim.MPI_Status_c2f_p.f_status = f_status;
	shim_pre( shim_id, &shim );
	shim.MPI_Status_c2f_p.rc = PMPI_Status_c2f(c_status, f_status);
	shim_post( shim_id, &shim );
	return shim.MPI_Status_c2f_p.rc;
}
#endif //USE_MPI_STATUS_C2F

//MPI_Status_f2c
#ifdef USE_MPI_STATUS_F2C
int
MPI_Status_f2c (int *f_status, MPI_Status *c_status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Status_f2c_p.shim_id = GMPI_STATUS_F2C;
	shim.MPI_Status_f2c_p.f_status = f_status;
	shim.MPI_Status_f2c_p.c_status = c_status;
	shim_pre( shim_id, &shim );
	shim.MPI_Status_f2c_p.rc = PMPI_Status_f2c(f_status, c_status);
	shim_post( shim_id, &shim );
	return shim.MPI_Status_f2c_p.rc;
}
#endif //USE_MPI_STATUS_F2C

//MPI_Status_set_cancelled
#ifdef USE_MPI_STATUS_SET_CANCELLED
int
MPI_Status_set_cancelled (MPI_Status *status, int flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Status_set_cancelled_p.shim_id = GMPI_STATUS_SET_CANCELLED;
	shim.MPI_Status_set_cancelled_p.status = status;
	shim.MPI_Status_set_cancelled_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Status_set_cancelled_p.rc = PMPI_Status_set_cancelled(status, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Status_set_cancelled_p.rc;
}
#endif //USE_MPI_STATUS_SET_CANCELLED

//MPI_Status_set_elements
#ifdef USE_MPI_STATUS_SET_ELEMENTS
int
MPI_Status_set_elements (MPI_Status *status, MPI_Datatype datatype, int count){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Status_set_elements_p.shim_id = GMPI_STATUS_SET_ELEMENTS;
	shim.MPI_Status_set_elements_p.status = status;
	shim.MPI_Status_set_elements_p.datatype = datatype;
	shim.MPI_Status_set_elements_p.count = count;
	shim_pre( shim_id, &shim );
	shim.MPI_Status_set_elements_p.rc = PMPI_Status_set_elements(status, datatype, count);
	shim_post( shim_id, &shim );
	return shim.MPI_Status_set_elements_p.rc;
}
#endif //USE_MPI_STATUS_SET_ELEMENTS

//MPI_Testall
#ifdef USE_MPI_TESTALL
int
MPI_Testall (int count, MPI_Request *array_of_requests, int *flag, MPI_Status *array_of_statuses){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Testall_p.shim_id = GMPI_TESTALL;
	shim.MPI_Testall_p.count = count;
	shim.MPI_Testall_p.array_of_requests = array_of_requests;
	shim.MPI_Testall_p.flag = flag;
	shim.MPI_Testall_p.array_of_statuses = array_of_statuses;
	shim_pre( shim_id, &shim );
	shim.MPI_Testall_p.rc = PMPI_Testall(count, array_of_requests, flag, array_of_statuses);
	shim_post( shim_id, &shim );
	return shim.MPI_Testall_p.rc;
}
#endif //USE_MPI_TESTALL

//MPI_Testany
#ifdef USE_MPI_TESTANY
int
MPI_Testany (int count, MPI_Request *array_of_requests, int *index, int *flag, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Testany_p.shim_id = GMPI_TESTANY;
	shim.MPI_Testany_p.count = count;
	shim.MPI_Testany_p.array_of_requests = array_of_requests;
	shim.MPI_Testany_p.index = index;
	shim.MPI_Testany_p.flag = flag;
	shim.MPI_Testany_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Testany_p.rc = PMPI_Testany(count, array_of_requests, index, flag, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Testany_p.rc;
}
#endif //USE_MPI_TESTANY

//MPI_Test
#ifdef USE_MPI_TEST
int
MPI_Test (MPI_Request *request, int *flag, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Test_p.shim_id = GMPI_TEST;
	shim.MPI_Test_p.request = request;
	shim.MPI_Test_p.flag = flag;
	shim.MPI_Test_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Test_p.rc = PMPI_Test(request, flag, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Test_p.rc;
}
#endif //USE_MPI_TEST

//MPI_Test_cancelled
#ifdef USE_MPI_TEST_CANCELLED
int
MPI_Test_cancelled (MPI_Status *status, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Test_cancelled_p.shim_id = GMPI_TEST_CANCELLED;
	shim.MPI_Test_cancelled_p.status = status;
	shim.MPI_Test_cancelled_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Test_cancelled_p.rc = PMPI_Test_cancelled(status, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Test_cancelled_p.rc;
}
#endif //USE_MPI_TEST_CANCELLED

//MPI_Testsome
#ifdef USE_MPI_TESTSOME
int
MPI_Testsome (int incount, MPI_Request *array_of_requests, int *outcount, int *array_of_indices, MPI_Status *array_of_statuses){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Testsome_p.shim_id = GMPI_TESTSOME;
	shim.MPI_Testsome_p.incount = incount;
	shim.MPI_Testsome_p.array_of_requests = array_of_requests;
	shim.MPI_Testsome_p.outcount = outcount;
	shim.MPI_Testsome_p.array_of_indices = array_of_indices;
	shim.MPI_Testsome_p.array_of_statuses = array_of_statuses;
	shim_pre( shim_id, &shim );
	shim.MPI_Testsome_p.rc = PMPI_Testsome(incount, array_of_requests, outcount, array_of_indices, array_of_statuses);
	shim_post( shim_id, &shim );
	return shim.MPI_Testsome_p.rc;
}
#endif //USE_MPI_TESTSOME

//MPI_Topo_test
#ifdef USE_MPI_TOPO_TEST
int
MPI_Topo_test (MPI_Comm comm, int *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Topo_test_p.shim_id = GMPI_TOPO_TEST;
	shim.MPI_Topo_test_p.comm = comm;
	shim.MPI_Topo_test_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Topo_test_p.rc = PMPI_Topo_test(comm, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Topo_test_p.rc;
}
#endif //USE_MPI_TOPO_TEST

//MPI_Type_c2f
#ifdef USE_MPI_TYPE_C2F
int
MPI_Type_c2f (MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_c2f_p.shim_id = GMPI_TYPE_C2F;
	shim.MPI_Type_c2f_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_c2f_p.rc = PMPI_Type_c2f(datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_c2f_p.rc;
}
#endif //USE_MPI_TYPE_C2F

//MPI_Type_commit
#ifdef USE_MPI_TYPE_COMMIT
int
MPI_Type_commit (MPI_Datatype *type){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_commit_p.shim_id = GMPI_TYPE_COMMIT;
	shim.MPI_Type_commit_p.type = type;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_commit_p.rc = PMPI_Type_commit(type);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_commit_p.rc;
}
#endif //USE_MPI_TYPE_COMMIT

//MPI_Type_contiguous
#ifdef USE_MPI_TYPE_CONTIGUOUS
int
MPI_Type_contiguous (int count, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_contiguous_p.shim_id = GMPI_TYPE_CONTIGUOUS;
	shim.MPI_Type_contiguous_p.count = count;
	shim.MPI_Type_contiguous_p.oldtype = oldtype;
	shim.MPI_Type_contiguous_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_contiguous_p.rc = PMPI_Type_contiguous(count, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_contiguous_p.rc;
}
#endif //USE_MPI_TYPE_CONTIGUOUS

//MPI_Type_create_darray
#ifdef USE_MPI_TYPE_CREATE_DARRAY
int
MPI_Type_create_darray (int size, int rank, int ndims, int *gsize_array, int *distrib_array, int *darg_array, int *psize_array, int order, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_darray_p.shim_id = GMPI_TYPE_CREATE_DARRAY;
	shim.MPI_Type_create_darray_p.size = size;
	shim.MPI_Type_create_darray_p.rank = rank;
	shim.MPI_Type_create_darray_p.ndims = ndims;
	shim.MPI_Type_create_darray_p.gsize_array = gsize_array;
	shim.MPI_Type_create_darray_p.distrib_array = distrib_array;
	shim.MPI_Type_create_darray_p.darg_array = darg_array;
	shim.MPI_Type_create_darray_p.psize_array = psize_array;
	shim.MPI_Type_create_darray_p.order = order;
	shim.MPI_Type_create_darray_p.oldtype = oldtype;
	shim.MPI_Type_create_darray_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_darray_p.rc = PMPI_Type_create_darray(size, rank, ndims, gsize_array, distrib_array, darg_array, psize_array, order, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_darray_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_DARRAY

//MPI_Type_create_f90_complex
#ifdef USE_MPI_TYPE_CREATE_F90_COMPLEX
int
MPI_Type_create_f90_complex (int p, int r, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_f90_complex_p.shim_id = GMPI_TYPE_CREATE_F90_COMPLEX;
	shim.MPI_Type_create_f90_complex_p.p = p;
	shim.MPI_Type_create_f90_complex_p.r = r;
	shim.MPI_Type_create_f90_complex_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_f90_complex_p.rc = PMPI_Type_create_f90_complex(p, r, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_f90_complex_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_F90_COMPLEX

//MPI_Type_create_f90_integer
#ifdef USE_MPI_TYPE_CREATE_F90_INTEGER
int
MPI_Type_create_f90_integer (int r, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_f90_integer_p.shim_id = GMPI_TYPE_CREATE_F90_INTEGER;
	shim.MPI_Type_create_f90_integer_p.r = r;
	shim.MPI_Type_create_f90_integer_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_f90_integer_p.rc = PMPI_Type_create_f90_integer(r, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_f90_integer_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_F90_INTEGER

//MPI_Type_create_f90_real
#ifdef USE_MPI_TYPE_CREATE_F90_REAL
int
MPI_Type_create_f90_real (int p, int r, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_f90_real_p.shim_id = GMPI_TYPE_CREATE_F90_REAL;
	shim.MPI_Type_create_f90_real_p.p = p;
	shim.MPI_Type_create_f90_real_p.r = r;
	shim.MPI_Type_create_f90_real_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_f90_real_p.rc = PMPI_Type_create_f90_real(p, r, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_f90_real_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_F90_REAL

//MPI_Type_create_hindexed
#ifdef USE_MPI_TYPE_CREATE_HINDEXED
int
MPI_Type_create_hindexed (int count, int *array_of_blocklengths, MPI_Aint *array_of_displacements, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_hindexed_p.shim_id = GMPI_TYPE_CREATE_HINDEXED;
	shim.MPI_Type_create_hindexed_p.count = count;
	shim.MPI_Type_create_hindexed_p.array_of_blocklengths = array_of_blocklengths;
	shim.MPI_Type_create_hindexed_p.array_of_displacements = array_of_displacements;
	shim.MPI_Type_create_hindexed_p.oldtype = oldtype;
	shim.MPI_Type_create_hindexed_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_hindexed_p.rc = PMPI_Type_create_hindexed(count, array_of_blocklengths, array_of_displacements, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_hindexed_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_HINDEXED

//MPI_Type_create_hvector
#ifdef USE_MPI_TYPE_CREATE_HVECTOR
int
MPI_Type_create_hvector (int count, int blocklength, MPI_Aint stride, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_hvector_p.shim_id = GMPI_TYPE_CREATE_HVECTOR;
	shim.MPI_Type_create_hvector_p.count = count;
	shim.MPI_Type_create_hvector_p.blocklength = blocklength;
	shim.MPI_Type_create_hvector_p.stride = stride;
	shim.MPI_Type_create_hvector_p.oldtype = oldtype;
	shim.MPI_Type_create_hvector_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_hvector_p.rc = PMPI_Type_create_hvector(count, blocklength, stride, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_hvector_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_HVECTOR

//MPI_Type_create_keyval
#ifdef USE_MPI_TYPE_CREATE_KEYVAL
int
MPI_Type_create_keyval (MPI_Type_copy_attr_function *type_copy_attr_fn, MPI_Type_delete_attr_function *type_delete_attr_fn, int *type_keyval, void *extra_state){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_keyval_p.shim_id = GMPI_TYPE_CREATE_KEYVAL;
	shim.MPI_Type_create_keyval_p.type_copy_attr_fn = type_copy_attr_fn;
	shim.MPI_Type_create_keyval_p.type_delete_attr_fn = type_delete_attr_fn;
	shim.MPI_Type_create_keyval_p.type_keyval = type_keyval;
	shim.MPI_Type_create_keyval_p.extra_state = extra_state;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_keyval_p.rc = PMPI_Type_create_keyval(type_copy_attr_fn, type_delete_attr_fn, type_keyval, extra_state);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_keyval_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_KEYVAL

//MPI_Type_create_indexed_block
#ifdef USE_MPI_TYPE_CREATE_INDEXED_BLOCK
int
MPI_Type_create_indexed_block (int count, int blocklength, int *array_of_displacements, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_indexed_block_p.shim_id = GMPI_TYPE_CREATE_INDEXED_BLOCK;
	shim.MPI_Type_create_indexed_block_p.count = count;
	shim.MPI_Type_create_indexed_block_p.blocklength = blocklength;
	shim.MPI_Type_create_indexed_block_p.array_of_displacements = array_of_displacements;
	shim.MPI_Type_create_indexed_block_p.oldtype = oldtype;
	shim.MPI_Type_create_indexed_block_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_indexed_block_p.rc = PMPI_Type_create_indexed_block(count, blocklength, array_of_displacements, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_indexed_block_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_INDEXED_BLOCK

//MPI_Type_create_struct
#ifdef USE_MPI_TYPE_CREATE_STRUCT
int
MPI_Type_create_struct (int count, int *array_of_block_lengths, MPI_Aint *array_of_displacements, MPI_Datatype *array_of_types, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_struct_p.shim_id = GMPI_TYPE_CREATE_STRUCT;
	shim.MPI_Type_create_struct_p.count = count;
	shim.MPI_Type_create_struct_p.array_of_block_lengths = array_of_block_lengths;
	shim.MPI_Type_create_struct_p.array_of_displacements = array_of_displacements;
	shim.MPI_Type_create_struct_p.array_of_types = array_of_types;
	shim.MPI_Type_create_struct_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_struct_p.rc = PMPI_Type_create_struct(count, array_of_block_lengths, array_of_displacements, array_of_types, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_struct_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_STRUCT

//MPI_Type_create_subarray
#ifdef USE_MPI_TYPE_CREATE_SUBARRAY
int
MPI_Type_create_subarray (int ndims, int *size_array, int *subsize_array, int *start_array, int order, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_subarray_p.shim_id = GMPI_TYPE_CREATE_SUBARRAY;
	shim.MPI_Type_create_subarray_p.ndims = ndims;
	shim.MPI_Type_create_subarray_p.size_array = size_array;
	shim.MPI_Type_create_subarray_p.subsize_array = subsize_array;
	shim.MPI_Type_create_subarray_p.start_array = start_array;
	shim.MPI_Type_create_subarray_p.order = order;
	shim.MPI_Type_create_subarray_p.oldtype = oldtype;
	shim.MPI_Type_create_subarray_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_subarray_p.rc = PMPI_Type_create_subarray(ndims, size_array, subsize_array, start_array, order, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_subarray_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_SUBARRAY

//MPI_Type_create_resized
#ifdef USE_MPI_TYPE_CREATE_RESIZED
int
MPI_Type_create_resized (MPI_Datatype oldtype, MPI_Aint lb, MPI_Aint extent, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_create_resized_p.shim_id = GMPI_TYPE_CREATE_RESIZED;
	shim.MPI_Type_create_resized_p.oldtype = oldtype;
	shim.MPI_Type_create_resized_p.lb = lb;
	shim.MPI_Type_create_resized_p.extent = extent;
	shim.MPI_Type_create_resized_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_create_resized_p.rc = PMPI_Type_create_resized(oldtype, lb, extent, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_create_resized_p.rc;
}
#endif //USE_MPI_TYPE_CREATE_RESIZED

//MPI_Type_delete_attr
#ifdef USE_MPI_TYPE_DELETE_ATTR
int
MPI_Type_delete_attr (MPI_Datatype type, int type_keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_delete_attr_p.shim_id = GMPI_TYPE_DELETE_ATTR;
	shim.MPI_Type_delete_attr_p.type = type;
	shim.MPI_Type_delete_attr_p.type_keyval = type_keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_delete_attr_p.rc = PMPI_Type_delete_attr(type, type_keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_delete_attr_p.rc;
}
#endif //USE_MPI_TYPE_DELETE_ATTR

//MPI_Type_dup
#ifdef USE_MPI_TYPE_DUP
int
MPI_Type_dup (MPI_Datatype type, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_dup_p.shim_id = GMPI_TYPE_DUP;
	shim.MPI_Type_dup_p.type = type;
	shim.MPI_Type_dup_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_dup_p.rc = PMPI_Type_dup(type, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_dup_p.rc;
}
#endif //USE_MPI_TYPE_DUP

//MPI_Type_extent
#ifdef USE_MPI_TYPE_EXTENT
int
MPI_Type_extent (MPI_Datatype type, MPI_Aint *extent){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_extent_p.shim_id = GMPI_TYPE_EXTENT;
	shim.MPI_Type_extent_p.type = type;
	shim.MPI_Type_extent_p.extent = extent;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_extent_p.rc = PMPI_Type_extent(type, extent);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_extent_p.rc;
}
#endif //USE_MPI_TYPE_EXTENT

//MPI_Type_free
#ifdef USE_MPI_TYPE_FREE
int
MPI_Type_free (MPI_Datatype *type){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_free_p.shim_id = GMPI_TYPE_FREE;
	shim.MPI_Type_free_p.type = type;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_free_p.rc = PMPI_Type_free(type);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_free_p.rc;
}
#endif //USE_MPI_TYPE_FREE

//MPI_Type_free_keyval
#ifdef USE_MPI_TYPE_FREE_KEYVAL
int
MPI_Type_free_keyval (int *type_keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_free_keyval_p.shim_id = GMPI_TYPE_FREE_KEYVAL;
	shim.MPI_Type_free_keyval_p.type_keyval = type_keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_free_keyval_p.rc = PMPI_Type_free_keyval(type_keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_free_keyval_p.rc;
}
#endif //USE_MPI_TYPE_FREE_KEYVAL

//MPI_Type_f2c
#ifdef USE_MPI_TYPE_F2C
MPI_Datatype
MPI_Type_f2c (int datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_f2c_p.shim_id = GMPI_TYPE_F2C;
	shim.MPI_Type_f2c_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_f2c_p.rc = PMPI_Type_f2c(datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_f2c_p.rc;
}
#endif //USE_MPI_TYPE_F2C

//MPI_Type_get_attr
#ifdef USE_MPI_TYPE_GET_ATTR
int
MPI_Type_get_attr (MPI_Datatype type, int type_keyval, void *attribute_val, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_get_attr_p.shim_id = GMPI_TYPE_GET_ATTR;
	shim.MPI_Type_get_attr_p.type = type;
	shim.MPI_Type_get_attr_p.type_keyval = type_keyval;
	shim.MPI_Type_get_attr_p.attribute_val = attribute_val;
	shim.MPI_Type_get_attr_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_get_attr_p.rc = PMPI_Type_get_attr(type, type_keyval, attribute_val, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_get_attr_p.rc;
}
#endif //USE_MPI_TYPE_GET_ATTR

//MPI_Type_get_contents
#ifdef USE_MPI_TYPE_GET_CONTENTS
int
MPI_Type_get_contents (MPI_Datatype mtype, int max_integers, int max_addresses, int max_datatypes, int *array_of_integers, MPI_Aint *array_of_addresses, MPI_Datatype *array_of_datatypes){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_get_contents_p.shim_id = GMPI_TYPE_GET_CONTENTS;
	shim.MPI_Type_get_contents_p.mtype = mtype;
	shim.MPI_Type_get_contents_p.max_integers = max_integers;
	shim.MPI_Type_get_contents_p.max_addresses = max_addresses;
	shim.MPI_Type_get_contents_p.max_datatypes = max_datatypes;
	shim.MPI_Type_get_contents_p.array_of_integers = array_of_integers;
	shim.MPI_Type_get_contents_p.array_of_addresses = array_of_addresses;
	shim.MPI_Type_get_contents_p.array_of_datatypes = array_of_datatypes;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_get_contents_p.rc = PMPI_Type_get_contents(mtype, max_integers, max_addresses, max_datatypes, array_of_integers, array_of_addresses, array_of_datatypes);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_get_contents_p.rc;
}
#endif //USE_MPI_TYPE_GET_CONTENTS

//MPI_Type_get_envelope
#ifdef USE_MPI_TYPE_GET_ENVELOPE
int
MPI_Type_get_envelope (MPI_Datatype type, int *num_integers, int *num_addresses, int *num_datatypes, int *combiner){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_get_envelope_p.shim_id = GMPI_TYPE_GET_ENVELOPE;
	shim.MPI_Type_get_envelope_p.type = type;
	shim.MPI_Type_get_envelope_p.num_integers = num_integers;
	shim.MPI_Type_get_envelope_p.num_addresses = num_addresses;
	shim.MPI_Type_get_envelope_p.num_datatypes = num_datatypes;
	shim.MPI_Type_get_envelope_p.combiner = combiner;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_get_envelope_p.rc = PMPI_Type_get_envelope(type, num_integers, num_addresses, num_datatypes, combiner);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_get_envelope_p.rc;
}
#endif //USE_MPI_TYPE_GET_ENVELOPE

//MPI_Type_get_extent
#ifdef USE_MPI_TYPE_GET_EXTENT
int
MPI_Type_get_extent (MPI_Datatype type, MPI_Aint *lb, MPI_Aint *extent){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_get_extent_p.shim_id = GMPI_TYPE_GET_EXTENT;
	shim.MPI_Type_get_extent_p.type = type;
	shim.MPI_Type_get_extent_p.lb = lb;
	shim.MPI_Type_get_extent_p.extent = extent;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_get_extent_p.rc = PMPI_Type_get_extent(type, lb, extent);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_get_extent_p.rc;
}
#endif //USE_MPI_TYPE_GET_EXTENT

//MPI_Type_get_name
#ifdef USE_MPI_TYPE_GET_NAME
int
MPI_Type_get_name (MPI_Datatype type, char *type_name, int *resultlen){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_get_name_p.shim_id = GMPI_TYPE_GET_NAME;
	shim.MPI_Type_get_name_p.type = type;
	shim.MPI_Type_get_name_p.type_name = type_name;
	shim.MPI_Type_get_name_p.resultlen = resultlen;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_get_name_p.rc = PMPI_Type_get_name(type, type_name, resultlen);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_get_name_p.rc;
}
#endif //USE_MPI_TYPE_GET_NAME

//MPI_Type_get_true_extent
#ifdef USE_MPI_TYPE_GET_TRUE_EXTENT
int
MPI_Type_get_true_extent (MPI_Datatype datatype, MPI_Aint *true_lb, MPI_Aint *true_extent){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_get_true_extent_p.shim_id = GMPI_TYPE_GET_TRUE_EXTENT;
	shim.MPI_Type_get_true_extent_p.datatype = datatype;
	shim.MPI_Type_get_true_extent_p.true_lb = true_lb;
	shim.MPI_Type_get_true_extent_p.true_extent = true_extent;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_get_true_extent_p.rc = PMPI_Type_get_true_extent(datatype, true_lb, true_extent);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_get_true_extent_p.rc;
}
#endif //USE_MPI_TYPE_GET_TRUE_EXTENT

//MPI_Type_hindexed
#ifdef USE_MPI_TYPE_HINDEXED
int
MPI_Type_hindexed (int count, int *array_of_blocklengths, MPI_Aint *array_of_displacements, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_hindexed_p.shim_id = GMPI_TYPE_HINDEXED;
	shim.MPI_Type_hindexed_p.count = count;
	shim.MPI_Type_hindexed_p.array_of_blocklengths = array_of_blocklengths;
	shim.MPI_Type_hindexed_p.array_of_displacements = array_of_displacements;
	shim.MPI_Type_hindexed_p.oldtype = oldtype;
	shim.MPI_Type_hindexed_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_hindexed_p.rc = PMPI_Type_hindexed(count, array_of_blocklengths, array_of_displacements, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_hindexed_p.rc;
}
#endif //USE_MPI_TYPE_HINDEXED

//MPI_Type_hvector
#ifdef USE_MPI_TYPE_HVECTOR
int
MPI_Type_hvector (int count, int blocklength, MPI_Aint stride, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_hvector_p.shim_id = GMPI_TYPE_HVECTOR;
	shim.MPI_Type_hvector_p.count = count;
	shim.MPI_Type_hvector_p.blocklength = blocklength;
	shim.MPI_Type_hvector_p.stride = stride;
	shim.MPI_Type_hvector_p.oldtype = oldtype;
	shim.MPI_Type_hvector_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_hvector_p.rc = PMPI_Type_hvector(count, blocklength, stride, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_hvector_p.rc;
}
#endif //USE_MPI_TYPE_HVECTOR

//MPI_Type_indexed
#ifdef USE_MPI_TYPE_INDEXED
int
MPI_Type_indexed (int count, int *array_of_blocklengths, int *array_of_displacements, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_indexed_p.shim_id = GMPI_TYPE_INDEXED;
	shim.MPI_Type_indexed_p.count = count;
	shim.MPI_Type_indexed_p.array_of_blocklengths = array_of_blocklengths;
	shim.MPI_Type_indexed_p.array_of_displacements = array_of_displacements;
	shim.MPI_Type_indexed_p.oldtype = oldtype;
	shim.MPI_Type_indexed_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_indexed_p.rc = PMPI_Type_indexed(count, array_of_blocklengths, array_of_displacements, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_indexed_p.rc;
}
#endif //USE_MPI_TYPE_INDEXED

//MPI_Type_lb
#ifdef USE_MPI_TYPE_LB
int
MPI_Type_lb (MPI_Datatype type, MPI_Aint *lb){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_lb_p.shim_id = GMPI_TYPE_LB;
	shim.MPI_Type_lb_p.type = type;
	shim.MPI_Type_lb_p.lb = lb;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_lb_p.rc = PMPI_Type_lb(type, lb);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_lb_p.rc;
}
#endif //USE_MPI_TYPE_LB

//MPI_Type_match_size
#ifdef USE_MPI_TYPE_MATCH_SIZE
int
MPI_Type_match_size (int typeclass, int size, MPI_Datatype *type){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_match_size_p.shim_id = GMPI_TYPE_MATCH_SIZE;
	shim.MPI_Type_match_size_p.typeclass = typeclass;
	shim.MPI_Type_match_size_p.size = size;
	shim.MPI_Type_match_size_p.type = type;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_match_size_p.rc = PMPI_Type_match_size(typeclass, size, type);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_match_size_p.rc;
}
#endif //USE_MPI_TYPE_MATCH_SIZE

//MPI_Type_set_attr
#ifdef USE_MPI_TYPE_SET_ATTR
int
MPI_Type_set_attr (MPI_Datatype type, int type_keyval, void *attr_val){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_set_attr_p.shim_id = GMPI_TYPE_SET_ATTR;
	shim.MPI_Type_set_attr_p.type = type;
	shim.MPI_Type_set_attr_p.type_keyval = type_keyval;
	shim.MPI_Type_set_attr_p.attr_val = attr_val;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_set_attr_p.rc = PMPI_Type_set_attr(type, type_keyval, attr_val);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_set_attr_p.rc;
}
#endif //USE_MPI_TYPE_SET_ATTR

//MPI_Type_set_name
#ifdef USE_MPI_TYPE_SET_NAME
int
MPI_Type_set_name (MPI_Datatype type, char *type_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_set_name_p.shim_id = GMPI_TYPE_SET_NAME;
	shim.MPI_Type_set_name_p.type = type;
	shim.MPI_Type_set_name_p.type_name = type_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_set_name_p.rc = PMPI_Type_set_name(type, type_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_set_name_p.rc;
}
#endif //USE_MPI_TYPE_SET_NAME

//MPI_Type_size
#ifdef USE_MPI_TYPE_SIZE
int
MPI_Type_size (MPI_Datatype type, int *size){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_size_p.shim_id = GMPI_TYPE_SIZE;
	shim.MPI_Type_size_p.type = type;
	shim.MPI_Type_size_p.size = size;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_size_p.rc = PMPI_Type_size(type, size);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_size_p.rc;
}
#endif //USE_MPI_TYPE_SIZE

//MPI_Type_struct
#ifdef USE_MPI_TYPE_STRUCT
int
MPI_Type_struct (int count, int *array_of_blocklengths, MPI_Aint *array_of_displacements, MPI_Datatype *array_of_types, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_struct_p.shim_id = GMPI_TYPE_STRUCT;
	shim.MPI_Type_struct_p.count = count;
	shim.MPI_Type_struct_p.array_of_blocklengths = array_of_blocklengths;
	shim.MPI_Type_struct_p.array_of_displacements = array_of_displacements;
	shim.MPI_Type_struct_p.array_of_types = array_of_types;
	shim.MPI_Type_struct_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_struct_p.rc = PMPI_Type_struct(count, array_of_blocklengths, array_of_displacements, array_of_types, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_struct_p.rc;
}
#endif //USE_MPI_TYPE_STRUCT

//MPI_Type_ub
#ifdef USE_MPI_TYPE_UB
int
MPI_Type_ub (MPI_Datatype mtype, MPI_Aint *ub){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_ub_p.shim_id = GMPI_TYPE_UB;
	shim.MPI_Type_ub_p.mtype = mtype;
	shim.MPI_Type_ub_p.ub = ub;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_ub_p.rc = PMPI_Type_ub(mtype, ub);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_ub_p.rc;
}
#endif //USE_MPI_TYPE_UB

//MPI_Type_vector
#ifdef USE_MPI_TYPE_VECTOR
int
MPI_Type_vector (int count, int blocklength, int stride, MPI_Datatype oldtype, MPI_Datatype *newtype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Type_vector_p.shim_id = GMPI_TYPE_VECTOR;
	shim.MPI_Type_vector_p.count = count;
	shim.MPI_Type_vector_p.blocklength = blocklength;
	shim.MPI_Type_vector_p.stride = stride;
	shim.MPI_Type_vector_p.oldtype = oldtype;
	shim.MPI_Type_vector_p.newtype = newtype;
	shim_pre( shim_id, &shim );
	shim.MPI_Type_vector_p.rc = PMPI_Type_vector(count, blocklength, stride, oldtype, newtype);
	shim_post( shim_id, &shim );
	return shim.MPI_Type_vector_p.rc;
}
#endif //USE_MPI_TYPE_VECTOR

//MPI_Unpack
#ifdef USE_MPI_UNPACK
int
MPI_Unpack (void *inbuf, int insize, int *position, void *outbuf, int outcount, MPI_Datatype datatype, MPI_Comm comm){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Unpack_p.shim_id = GMPI_UNPACK;
	shim.MPI_Unpack_p.inbuf = inbuf;
	shim.MPI_Unpack_p.insize = insize;
	shim.MPI_Unpack_p.position = position;
	shim.MPI_Unpack_p.outbuf = outbuf;
	shim.MPI_Unpack_p.outcount = outcount;
	shim.MPI_Unpack_p.datatype = datatype;
	shim.MPI_Unpack_p.comm = comm;
	shim_pre( shim_id, &shim );
	shim.MPI_Unpack_p.rc = PMPI_Unpack(inbuf, insize, position, outbuf, outcount, datatype, comm);
	shim_post( shim_id, &shim );
	return shim.MPI_Unpack_p.rc;
}
#endif //USE_MPI_UNPACK

//MPI_Unpublish_name
#ifdef USE_MPI_UNPUBLISH_NAME
int
MPI_Unpublish_name (char *service_name, MPI_Info info, char *port_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Unpublish_name_p.shim_id = GMPI_UNPUBLISH_NAME;
	shim.MPI_Unpublish_name_p.service_name = service_name;
	shim.MPI_Unpublish_name_p.info = info;
	shim.MPI_Unpublish_name_p.port_name = port_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Unpublish_name_p.rc = PMPI_Unpublish_name(service_name, info, port_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Unpublish_name_p.rc;
}
#endif //USE_MPI_UNPUBLISH_NAME

//MPI_Unpack_external
#ifdef USE_MPI_UNPACK_EXTERNAL
int
MPI_Unpack_external (char *datarep, void *inbuf, MPI_Aint insize, MPI_Aint *position, void *outbuf, int outcount, MPI_Datatype datatype){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Unpack_external_p.shim_id = GMPI_UNPACK_EXTERNAL;
	shim.MPI_Unpack_external_p.datarep = datarep;
	shim.MPI_Unpack_external_p.inbuf = inbuf;
	shim.MPI_Unpack_external_p.insize = insize;
	shim.MPI_Unpack_external_p.position = position;
	shim.MPI_Unpack_external_p.outbuf = outbuf;
	shim.MPI_Unpack_external_p.outcount = outcount;
	shim.MPI_Unpack_external_p.datatype = datatype;
	shim_pre( shim_id, &shim );
	shim.MPI_Unpack_external_p.rc = PMPI_Unpack_external(datarep, inbuf, insize, position, outbuf, outcount, datatype);
	shim_post( shim_id, &shim );
	return shim.MPI_Unpack_external_p.rc;
}
#endif //USE_MPI_UNPACK_EXTERNAL

//MPI_Waitall
#ifdef USE_MPI_WAITALL
int
MPI_Waitall (int count, MPI_Request *array_of_requests, MPI_Status *array_of_statuses){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Waitall_p.shim_id = GMPI_WAITALL;
	shim.MPI_Waitall_p.count = count;
	shim.MPI_Waitall_p.array_of_requests = array_of_requests;
	shim.MPI_Waitall_p.array_of_statuses = array_of_statuses;
	shim_pre( shim_id, &shim );
	shim.MPI_Waitall_p.rc = PMPI_Waitall(count, array_of_requests, array_of_statuses);
	shim_post( shim_id, &shim );
	return shim.MPI_Waitall_p.rc;
}
#endif //USE_MPI_WAITALL

//MPI_Waitany
#ifdef USE_MPI_WAITANY
int
MPI_Waitany (int count, MPI_Request *array_of_requests, int *index, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Waitany_p.shim_id = GMPI_WAITANY;
	shim.MPI_Waitany_p.count = count;
	shim.MPI_Waitany_p.array_of_requests = array_of_requests;
	shim.MPI_Waitany_p.index = index;
	shim.MPI_Waitany_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Waitany_p.rc = PMPI_Waitany(count, array_of_requests, index, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Waitany_p.rc;
}
#endif //USE_MPI_WAITANY

//MPI_Wait
#ifdef USE_MPI_WAIT
int
MPI_Wait (MPI_Request *request, MPI_Status *status){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Wait_p.shim_id = GMPI_WAIT;
	shim.MPI_Wait_p.request = request;
	shim.MPI_Wait_p.status = status;
	shim_pre( shim_id, &shim );
	shim.MPI_Wait_p.rc = PMPI_Wait(request, status);
	shim_post( shim_id, &shim );
	return shim.MPI_Wait_p.rc;
}
#endif //USE_MPI_WAIT

//MPI_Waitsome
#ifdef USE_MPI_WAITSOME
int
MPI_Waitsome (int incount, MPI_Request *array_of_requests, int *outcount, int *array_of_indices, MPI_Status *array_of_statuses){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Waitsome_p.shim_id = GMPI_WAITSOME;
	shim.MPI_Waitsome_p.incount = incount;
	shim.MPI_Waitsome_p.array_of_requests = array_of_requests;
	shim.MPI_Waitsome_p.outcount = outcount;
	shim.MPI_Waitsome_p.array_of_indices = array_of_indices;
	shim.MPI_Waitsome_p.array_of_statuses = array_of_statuses;
	shim_pre( shim_id, &shim );
	shim.MPI_Waitsome_p.rc = PMPI_Waitsome(incount, array_of_requests, outcount, array_of_indices, array_of_statuses);
	shim_post( shim_id, &shim );
	return shim.MPI_Waitsome_p.rc;
}
#endif //USE_MPI_WAITSOME

//MPI_Win_c2f
#ifdef USE_MPI_WIN_C2F
int
MPI_Win_c2f (MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_c2f_p.shim_id = GMPI_WIN_C2F;
	shim.MPI_Win_c2f_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_c2f_p.rc = PMPI_Win_c2f(win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_c2f_p.rc;
}
#endif //USE_MPI_WIN_C2F

//MPI_Win_call_errhandler
#ifdef USE_MPI_WIN_CALL_ERRHANDLER
int
MPI_Win_call_errhandler (MPI_Win win, int errorcode){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_call_errhandler_p.shim_id = GMPI_WIN_CALL_ERRHANDLER;
	shim.MPI_Win_call_errhandler_p.win = win;
	shim.MPI_Win_call_errhandler_p.errorcode = errorcode;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_call_errhandler_p.rc = PMPI_Win_call_errhandler(win, errorcode);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_call_errhandler_p.rc;
}
#endif //USE_MPI_WIN_CALL_ERRHANDLER

//MPI_Win_complete
#ifdef USE_MPI_WIN_COMPLETE
int
MPI_Win_complete (MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_complete_p.shim_id = GMPI_WIN_COMPLETE;
	shim.MPI_Win_complete_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_complete_p.rc = PMPI_Win_complete(win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_complete_p.rc;
}
#endif //USE_MPI_WIN_COMPLETE

//MPI_Win_create
#ifdef USE_MPI_WIN_CREATE
int
MPI_Win_create (void *base, MPI_Aint size, int disp_unit, MPI_Info info, MPI_Comm comm, MPI_Win *win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_create_p.shim_id = GMPI_WIN_CREATE;
	shim.MPI_Win_create_p.base = base;
	shim.MPI_Win_create_p.size = size;
	shim.MPI_Win_create_p.disp_unit = disp_unit;
	shim.MPI_Win_create_p.info = info;
	shim.MPI_Win_create_p.comm = comm;
	shim.MPI_Win_create_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_create_p.rc = PMPI_Win_create(base, size, disp_unit, info, comm, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_create_p.rc;
}
#endif //USE_MPI_WIN_CREATE

//MPI_Win_create_errhandler
#ifdef USE_MPI_WIN_CREATE_ERRHANDLER
int
MPI_Win_create_errhandler (MPI_Win_errhandler_fn *function, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_create_errhandler_p.shim_id = GMPI_WIN_CREATE_ERRHANDLER;
	shim.MPI_Win_create_errhandler_p.function = function;
	shim.MPI_Win_create_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_create_errhandler_p.rc = PMPI_Win_create_errhandler(function, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_create_errhandler_p.rc;
}
#endif //USE_MPI_WIN_CREATE_ERRHANDLER

//MPI_Win_create_keyval
#ifdef USE_MPI_WIN_CREATE_KEYVAL
int
MPI_Win_create_keyval (MPI_Win_copy_attr_function *win_copy_attr_fn, MPI_Win_delete_attr_function *win_delete_attr_fn, int *win_keyval, void *extra_state){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_create_keyval_p.shim_id = GMPI_WIN_CREATE_KEYVAL;
	shim.MPI_Win_create_keyval_p.win_copy_attr_fn = win_copy_attr_fn;
	shim.MPI_Win_create_keyval_p.win_delete_attr_fn = win_delete_attr_fn;
	shim.MPI_Win_create_keyval_p.win_keyval = win_keyval;
	shim.MPI_Win_create_keyval_p.extra_state = extra_state;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_create_keyval_p.rc = PMPI_Win_create_keyval(win_copy_attr_fn, win_delete_attr_fn, win_keyval, extra_state);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_create_keyval_p.rc;
}
#endif //USE_MPI_WIN_CREATE_KEYVAL

//MPI_Win_delete_attr
#ifdef USE_MPI_WIN_DELETE_ATTR
int
MPI_Win_delete_attr (MPI_Win win, int win_keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_delete_attr_p.shim_id = GMPI_WIN_DELETE_ATTR;
	shim.MPI_Win_delete_attr_p.win = win;
	shim.MPI_Win_delete_attr_p.win_keyval = win_keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_delete_attr_p.rc = PMPI_Win_delete_attr(win, win_keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_delete_attr_p.rc;
}
#endif //USE_MPI_WIN_DELETE_ATTR

//MPI_Win_f2c
#ifdef USE_MPI_WIN_F2C
MPI_Win
MPI_Win_f2c (int win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_f2c_p.shim_id = GMPI_WIN_F2C;
	shim.MPI_Win_f2c_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_f2c_p.rc = PMPI_Win_f2c(win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_f2c_p.rc;
}
#endif //USE_MPI_WIN_F2C

//MPI_Win_fence
#ifdef USE_MPI_WIN_FENCE
int
MPI_Win_fence (int assert, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_fence_p.shim_id = GMPI_WIN_FENCE;
	shim.MPI_Win_fence_p.assert = assert;
	shim.MPI_Win_fence_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_fence_p.rc = PMPI_Win_fence(assert, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_fence_p.rc;
}
#endif //USE_MPI_WIN_FENCE

//MPI_Win_free
#ifdef USE_MPI_WIN_FREE
int
MPI_Win_free (MPI_Win *win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_free_p.shim_id = GMPI_WIN_FREE;
	shim.MPI_Win_free_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_free_p.rc = PMPI_Win_free(win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_free_p.rc;
}
#endif //USE_MPI_WIN_FREE

//MPI_Win_free_keyval
#ifdef USE_MPI_WIN_FREE_KEYVAL
int
MPI_Win_free_keyval (int *win_keyval){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_free_keyval_p.shim_id = GMPI_WIN_FREE_KEYVAL;
	shim.MPI_Win_free_keyval_p.win_keyval = win_keyval;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_free_keyval_p.rc = PMPI_Win_free_keyval(win_keyval);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_free_keyval_p.rc;
}
#endif //USE_MPI_WIN_FREE_KEYVAL

//MPI_Win_get_attr
#ifdef USE_MPI_WIN_GET_ATTR
int
MPI_Win_get_attr (MPI_Win win, int win_keyval, void *attribute_val, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_get_attr_p.shim_id = GMPI_WIN_GET_ATTR;
	shim.MPI_Win_get_attr_p.win = win;
	shim.MPI_Win_get_attr_p.win_keyval = win_keyval;
	shim.MPI_Win_get_attr_p.attribute_val = attribute_val;
	shim.MPI_Win_get_attr_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_get_attr_p.rc = PMPI_Win_get_attr(win, win_keyval, attribute_val, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_get_attr_p.rc;
}
#endif //USE_MPI_WIN_GET_ATTR

//MPI_Win_get_errhandler
#ifdef USE_MPI_WIN_GET_ERRHANDLER
int
MPI_Win_get_errhandler (MPI_Win win, MPI_Errhandler *errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_get_errhandler_p.shim_id = GMPI_WIN_GET_ERRHANDLER;
	shim.MPI_Win_get_errhandler_p.win = win;
	shim.MPI_Win_get_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_get_errhandler_p.rc = PMPI_Win_get_errhandler(win, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_get_errhandler_p.rc;
}
#endif //USE_MPI_WIN_GET_ERRHANDLER

//MPI_Win_get_group
#ifdef USE_MPI_WIN_GET_GROUP
int
MPI_Win_get_group (MPI_Win win, MPI_Group *group){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_get_group_p.shim_id = GMPI_WIN_GET_GROUP;
	shim.MPI_Win_get_group_p.win = win;
	shim.MPI_Win_get_group_p.group = group;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_get_group_p.rc = PMPI_Win_get_group(win, group);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_get_group_p.rc;
}
#endif //USE_MPI_WIN_GET_GROUP

//MPI_Win_get_name
#ifdef USE_MPI_WIN_GET_NAME
int
MPI_Win_get_name (MPI_Win win, char *win_name, int *resultlen){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_get_name_p.shim_id = GMPI_WIN_GET_NAME;
	shim.MPI_Win_get_name_p.win = win;
	shim.MPI_Win_get_name_p.win_name = win_name;
	shim.MPI_Win_get_name_p.resultlen = resultlen;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_get_name_p.rc = PMPI_Win_get_name(win, win_name, resultlen);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_get_name_p.rc;
}
#endif //USE_MPI_WIN_GET_NAME

//MPI_Win_lock
#ifdef USE_MPI_WIN_LOCK
int
MPI_Win_lock (int lock_type, int rank, int assert, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_lock_p.shim_id = GMPI_WIN_LOCK;
	shim.MPI_Win_lock_p.lock_type = lock_type;
	shim.MPI_Win_lock_p.rank = rank;
	shim.MPI_Win_lock_p.assert = assert;
	shim.MPI_Win_lock_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_lock_p.rc = PMPI_Win_lock(lock_type, rank, assert, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_lock_p.rc;
}
#endif //USE_MPI_WIN_LOCK

//MPI_Win_post
#ifdef USE_MPI_WIN_POST
int
MPI_Win_post (MPI_Group group, int assert, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_post_p.shim_id = GMPI_WIN_POST;
	shim.MPI_Win_post_p.group = group;
	shim.MPI_Win_post_p.assert = assert;
	shim.MPI_Win_post_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_post_p.rc = PMPI_Win_post(group, assert, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_post_p.rc;
}
#endif //USE_MPI_WIN_POST

//MPI_Win_set_attr
#ifdef USE_MPI_WIN_SET_ATTR
int
MPI_Win_set_attr (MPI_Win win, int win_keyval, void *attribute_val){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_set_attr_p.shim_id = GMPI_WIN_SET_ATTR;
	shim.MPI_Win_set_attr_p.win = win;
	shim.MPI_Win_set_attr_p.win_keyval = win_keyval;
	shim.MPI_Win_set_attr_p.attribute_val = attribute_val;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_set_attr_p.rc = PMPI_Win_set_attr(win, win_keyval, attribute_val);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_set_attr_p.rc;
}
#endif //USE_MPI_WIN_SET_ATTR

//MPI_Win_set_errhandler
#ifdef USE_MPI_WIN_SET_ERRHANDLER
int
MPI_Win_set_errhandler (MPI_Win win, MPI_Errhandler errhandler){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_set_errhandler_p.shim_id = GMPI_WIN_SET_ERRHANDLER;
	shim.MPI_Win_set_errhandler_p.win = win;
	shim.MPI_Win_set_errhandler_p.errhandler = errhandler;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_set_errhandler_p.rc = PMPI_Win_set_errhandler(win, errhandler);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_set_errhandler_p.rc;
}
#endif //USE_MPI_WIN_SET_ERRHANDLER

//MPI_Win_set_name
#ifdef USE_MPI_WIN_SET_NAME
int
MPI_Win_set_name (MPI_Win win, char *win_name){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_set_name_p.shim_id = GMPI_WIN_SET_NAME;
	shim.MPI_Win_set_name_p.win = win;
	shim.MPI_Win_set_name_p.win_name = win_name;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_set_name_p.rc = PMPI_Win_set_name(win, win_name);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_set_name_p.rc;
}
#endif //USE_MPI_WIN_SET_NAME

//MPI_Win_start
#ifdef USE_MPI_WIN_START
int
MPI_Win_start (MPI_Group group, int assert, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_start_p.shim_id = GMPI_WIN_START;
	shim.MPI_Win_start_p.group = group;
	shim.MPI_Win_start_p.assert = assert;
	shim.MPI_Win_start_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_start_p.rc = PMPI_Win_start(group, assert, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_start_p.rc;
}
#endif //USE_MPI_WIN_START

//MPI_Win_test
#ifdef USE_MPI_WIN_TEST
int
MPI_Win_test (MPI_Win win, int *flag){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_test_p.shim_id = GMPI_WIN_TEST;
	shim.MPI_Win_test_p.win = win;
	shim.MPI_Win_test_p.flag = flag;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_test_p.rc = PMPI_Win_test(win, flag);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_test_p.rc;
}
#endif //USE_MPI_WIN_TEST

//MPI_Win_unlock
#ifdef USE_MPI_WIN_UNLOCK
int
MPI_Win_unlock (int rank, MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_unlock_p.shim_id = GMPI_WIN_UNLOCK;
	shim.MPI_Win_unlock_p.rank = rank;
	shim.MPI_Win_unlock_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_unlock_p.rc = PMPI_Win_unlock(rank, win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_unlock_p.rc;
}
#endif //USE_MPI_WIN_UNLOCK

//MPI_Win_wait
#ifdef USE_MPI_WIN_WAIT
int
MPI_Win_wait (MPI_Win win){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Win_wait_p.shim_id = GMPI_WIN_WAIT;
	shim.MPI_Win_wait_p.win = win;
	shim_pre( shim_id, &shim );
	shim.MPI_Win_wait_p.rc = PMPI_Win_wait(win);
	shim_post( shim_id, &shim );
	return shim.MPI_Win_wait_p.rc;
}
#endif //USE_MPI_WIN_WAIT

//MPI_Wtick
#ifdef USE_MPI_WTICK
double
MPI_Wtick (void){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Wtick_p.shim_id = GMPI_WTICK;
	shim_pre( shim_id, &shim );
	shim.MPI_Wtick_p.rc = PMPI_Wtick();
	shim_post( shim_id, &shim );
	return shim.MPI_Wtick_p.rc;
}
#endif //USE_MPI_WTICK

//MPI_Wtime
#ifdef USE_MPI_WTIME
double
MPI_Wtime (void){
	union shim_parameters shim;
	int shim_id;
	shim_id = shim.MPI_Wtime_p.shim_id = GMPI_WTIME;
	shim_pre( shim_id, &shim );
	shim.MPI_Wtime_p.rc = PMPI_Wtime();
	shim_post( shim_id, &shim );
	return shim.MPI_Wtime_p.rc;
}
#endif //USE_MPI_WTIME
